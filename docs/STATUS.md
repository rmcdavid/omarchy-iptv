# Omarchy IPTV - Status Board

Owner: Project Manager. Living document; every role updates its own rows.
Task definitions, dependencies, gates: docs/PLAN.md. Last PM update:
2026-09-15 (defect-ledger reconciliation; the previous line still read
2026-09-12, over a board that had gained M2-03, M2-05, M2-09 and three
releases since).

States: todo | doing | blocked | review | done.
`done` requires evidence in the row or in Handoff notes (PLAN.md section 7).
Rule: FE does not start a task whose Deps (PLAN.md) are not all `done` here.

## Board

### M0 Foundations

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M0-01 | PRODUCT.md | PO | done | docs/PRODUCT.md v0.1, 2026-09-12 |
| M0-02 | Plugin contract digest | PO | done | docs/OMARCHY-PLUGIN-CONTRACT.md, facts verified on this machine |
| M0-03 | PLAN.md + STATUS.md | PM | done | docs/PLAN.md (273 lines), docs/STATUS.md; ASCII-only checked |
| M0-04 | ARCHITECTURE.md | ARCH | done | docs/ARCHITECTURE.md, commit 42015dc; PO rulings R1-R13 appended as section 12 |
| M0-05 | UX.md | UX | done | docs/UX.md (918 lines), commit d9a1916 |
| M0-06 | Scaffold passing validate + check.sh | ARCH | done | commit 42015dc; validate exit 0; check.sh all green (61 node checks, 28 python tests, 8 qml) |
| M0-07 | Schema freeze / interface review | PM+ARCH+UX | done | PO reconciled ARCH vs UX: ARCHITECTURE.md section 12 (R1-R13); manifest updated (refreshMinutes 360, barLabelMaxWidth) |
| M0-08 | QA test plan + fixtures (docs/QA.md, tests/fixtures/) | QA | done | merged c0d5f42: docs/QA.md (174 cases), qa- fixtures, scripts/gen-playlist.py, scripts/qa-live.sh; 18 scaffold defects pre-filed (4 P2) and routed to the lanes |
| M0-09 | QA-ASSETS.md (public playlists, EPG sources) | PO | done | docs/QA-ASSETS.md, committed 0157fd6 |

### M1 MVP - Lane A (helper)

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M1-01 | Helper skeleton, paths, atomic writes, status.json || FE  | done | merged f03fef2 (lane A); structured errors, redaction, atomic 0600 writes ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-02 | M3U parser + tests || FE  | done | merged f03fef2; 61 parser tests incl. QA fixtures; 10k parse 467 ms ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-03 | Playlist fetch + cache + stale handling || FE  | done | merged f03fef2; codes bad_url, not_a_playlist added ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-04 | XMLTV EPG streaming parse + now/next || FE  | done | merged f03fef2; --now seam, --now-only 75 ms for 10k, private window cache ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-05 | mpv control over IPC socket || FE  | done | merged f03fef2; play/stop/status with fake-mpv tests (23) ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-06 | State file (favorites, recents) || FE  | done | merged f03fef2; helper `state` subcommand + Service.qml FileView ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|

### M1 MVP - Lane B (Model.js + QML)

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M1-07 | Model.js + Model.test.js || FE  | done | merged 514acef (lane B): 244 node checks; awaiting QA ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-08 | Overlay shell (window, focus, open/close) || FE  | done | merged 514acef; harness verified focus and Esc semantics ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-09 | Overlay guide (search, groups, list, states) || FE  | done | merged 514acef; two-mode keys, group column, all states in harness ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-10 | Overlay EPG rows + favorites/recents || FE  | done | merged 514acef; fake-EPG rows verified in harness ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-11 | Bar widget (glyph, label, clicks, scroll) || FE  | done | merged 514acef; wheel zap not exercised live yet ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-12 | Settings propagation || FE  | done | merged 514acef; shell.barConfig path, needs live check ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|

### M1 MVP - Join

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M1-13 | Refresh scheduler + cached hint || FE  | done | merged 514acef (Service.qml timers, cached banner) ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-14 | Notifications || FE  | done | merged 514acef; URL-redacted per D-QA-01 ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-15 | Playback wiring end to end || FE  | done | both lanes merged f03fef2; live verification pending ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-16 | IpcHandler commands || FE  | done | merged 514acef: toggle play stop next previous refresh status ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-17 | QML unit tests (spec.qml) || FE  | done | Model.spec.qml 16 passed; Guide/BarWidget specs not possible under qmltestrunner (PanelWindow), harness covers them ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-18 | README || FE (UX review)  | done | PO rewrote README e03f2f8 + contrib/ snippets ; accepted at REL-04 (PO, 2026-09-13, US1-US8 verified on harness and live shell)|
| M1-19 | scripts/check.sh green | FE | done | f03fef2: validate ok, 244 node, 127 python, 16 qml, ascii ok |
| M1-20 | FE smoke on live shell (US1-US8 once) | FE | superseded by M1.1-01 | This was the FE gate that had to pass before QA ran the full matrix. QA then ran that matrix on the live shell (M1.1-01, done) and every release since has had its own live pass on this machine: v0.2.1 P1 verification, the M2-02 player pass, the PiP pass, and the M2-09 guide-at-scale pass. Closing an unexecuted gate whose successor has passed six times. Not re-run |

### M1.1 Hardening

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M1.1-01 | Live-shell functional QA (docs/QA.md matrix) | QA | done | harness: 148 pass / 0 fail; live shell 2026-09-13: install, enable, bar set, guide, IPC play/stop, dead stream, keybinding, menu row, restart shell all pass (QA-RESULTS.md live section) |
| M1.1-02 | Theme-switch check | QA | done | live: Retropc -> Nord with the guide open re-skinned without restart; restored |
| M1.1-03 | Performance check (10k playlist, large EPG) | QA | done | all within budget: playlist 11k parse 530 ms, overlay open ~64 ms net on 11k cache, filter <= 6.7 ms, EPG 41 MB gz 2.9 s, --now-only 37 ms |
| M1.1-04 | Security review (G4) | ARCH | done | docs/SECURITY-REVIEW.md: pass with findings (S-01 P2, 7 P3); all eight fixed and merged 134fbe1 (256 node, 144 python) |
| M1.1-05 | Fix round 1 (P1/P2) | FE | done | security round merged 134fbe1; QA-defect round merged 2ce0b52; all 23 items verified by M1.1-06 |
| M1.1-06 | Regression re-test | QA | done | QA-RESULTS.md: regression on 2ce0b52 (23/23 fixes, 0 regressions) and release regression on 502f4b3 (D-LIVE-16..18 verified, 120 cases, 0 regressions) |
| M1.1-07 | Docs polish + known limitations | FE+UX | done | README (limits, disable note, third-party bar, behavior notes, D-LIVE-18), UX 6.3/6.4 wording, ARCHITECTURE 12.1, CHANGELOG.md |
| M1.1-08 | Dead-stream hardening | FE | done | merged 502f4b3: column positioning, SIGTERM->SIGKILL stop ladder, playlist warnings in footer; verified by QA |

### Release

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| REL-01 | Clean install via git clone | QA | done | live machine 2026-09-13: clone, validate, rescan, enable, bar set; then fast-forwarded to the release build and shell restarted (QA-RESULTS.md live section) |
| REL-02 | Uninstall leaves only cache + state | QA | done (by inspection) | not executed on the live machine to keep the delivered install; runtime writes verified limited to cache, state, runtime dirs (harness + code review); README Uninstall documents all three |
| REL-03 | Version 0.1.0 + CHANGELOG + final check.sh | FE | done | manifest 0.1.0; CHANGELOG.md 0.1.0 section; check.sh all green on 502f4b3 (299 node, 144 python, 21 qml) |
| REL-04 | PO acceptance vs PRODUCT.md | PO | done | US1-US8 verified (harness + live shell); quality bar met except multi-monitor (single output available); 0 open P1/P2; accepted 2026-09-13 |
| REL-05 | Tag v0.1.0 + push + re-install at tag | PO | done | tag v0.1.0 created; no remote configured (gh not logged in), push deferred to the user; installed clone fast-forwarded to the tag |

### M2 Backlog (unscheduled)

| Task ID | Title | Owner | State | Evidence / Notes |
|---|---|---|---|---|
| M2-01 | Sources screen: in-guide playlist entry, source history, per-source cache, Xtream form (docs/M2-SOURCES.md) | PO+UX+ARCH -> FE -> QA | done | fix round merged d50e364 (QA regression 10/10, 190/196 pass); released as v0.2.0; the live-shell runbook on ec4f702 found two P1s (D-LIVE-20/21), fixed at 845d445 and both verified fixed on the live shell with their original repros (QA-RESULTS "P1 fix verification on 845d445"). 0 open P1/P2; awaiting the v0.2.1 tag |
| M2-02 | Detached player: survives `omarchy restart shell`, no stream URL on any command line (docs/ARCHITECTURE-PLAYER.md) | ARCH -> FE -> QA | done (QA fix verification done, **go**) | design accepted with PO-1..PO-7; gate PA-0 passed on real mpv and gate PB-0 (docs/SPIKE-QUICKSHELL-SOCKET.md) withdrew the prescribed socket shape, amended in section 13; lanes PA (5fc9927) and PB (10eac45) and the fix lane (c458ebf) all merged. **Live QA pass on 8f9447e (QA-RESULTS "M2-02 player pass on 8f9447e"): 96/129 pass, 7 defects, 1 P1.** The headline case is proven - same mpv pid, same start time, one window and full identity including `launchedFrom` across ten consecutive restarts and across a SIGKILLed shell - and S-03 is closed on the first channel and after ten zaps. Blocked on **D-PLY-1** (P1: the interface goes idle while a respawned player keeps playing) and **D-PLY-2** (PO-2's `remove` acceptance case). The ARCH-P section 15 startup race was gated and **reproduces at 47%** (D-PLY-4); the lead owns that fix decision. **Fix verification on b16b479 (QA-RESULTS "M2-02 fix verification on b16b479"): all seven defects discharged - five fixed, D-PLY-4 re-measured by QA at 30 won / 0 lost, D-PLY-2 closed as accepted under PO-8 with its documented recovery tested verbatim.** Every performance budget met, including the wedged-stop one that failed before (4236-4263 ms against 4500). Four new P3s filed (D-PLY-8..11), none gating. **Go for v0.3.0** |
| M2-02x | Multiple playlists | - | superseded by M2-01 (PO-7) | the source history is the multi-playlist model; the M2-02 id now means the detached player |
| M2-03 | Channel numbers + numeric zap | ARCH -> FE -> QA | done | built in two lanes, integrated with zero stand-ins, released as v0.4.0 at 32044ca. The live pass at 0ef73ed found two P2s (D-CHNO-1, D-CHNO-2) and both were fixed afterwards at 7a898f1 and de6e57a. **Note the order: the live pass is what FOUND them, and no pass has run since**, so both rows read `fixed`, not `verified fixed`. The user's own provider carries no channel numbers, so the feature shows the no-numbers message for them |
| M2-04 | Channel logos | - | todo (value in question) | Measured on 2026-09-15 across all four of the subscriber's own playlists (5,221 channels): **1,396 carry a non-empty `tvg-logo`, 27 percent.** By playlist: USChannels 737/3,335 (22 percent), SportsPPVAll 606/1,833 (33 percent), Kids 48/48, Default 5/5 - so the two lists they actually browse are the two with poor coverage, and the two with full coverage are 53 channels between them. **All 1,396 point at a single third-party host over https (`logo.m3uassets.com`)**, which means enabling logos sends that host a request per visible row, tied to this user's viewing, and puts an image cache on disk. That is the first outbound fetch in this plugin to anything but the user's own provider and EPG. PO decision needed on whether 27 percent coverage justifies the privacy cost, before any design work. If it goes ahead, the design has to lead with the four-in-five placeholder case, not the happy one |
| M2-05 | PiP via hyprctl float + pin | ARCH -> FE -> QA | done | released as v0.5.0 at db47c37. The gate at c4d4075 disproved two design assumptions and found **D-PIP-1**, a P2 in already-released code whose own test was green on the bug and red on the fix. Integration found three rules implemented twice. Live pass on 5e3a374 (QA-RESULTS "M2-05 picture-in-picture LIVE pass"): zero P1, D-PIP-1 confirmed fixed on a real window, three new defects filed and all three fixed before the release (0d8f2ad, d3aece2, 043bebd). **D-PIP-3 remains open on its own terms per ruling PIP5**: the player window renders translucent because Omarchy's media-opacity exemption matches on class and ours does not match it |
| M2-06 | Recording via ffmpeg | - | todo | Large. Introduces a second long-lived child process with a different lifecycle from the player, writes files of unbounded size outside the cache, and needs its own failure surface. Not scoped |
| M2-07 | Catch-up / timeshift | - | **closed, no source exists** | Requires a provider that exposes catch-up. Checked on 2026-09-15: **none of the subscriber's four playlists carries a single `catchup`, `catchup-source`, `catchup-days`, `timeshift` or `tvg-rec` attribute** across 5,221 channels. We cannot verify one acceptance case against real data, and a feature this size built only against fixtures is how the v0.2.0 P1s happened. Blocked on evidence, not on effort. **CLOSED 2026-09-16 after phase 1**: the user probed their provider's panel directly and all three API requests returned the identical 748-byte HTML page, so the server is not parsing the query and there is no interface behind it. The M3U carries exactly four attribute keys across all 5,221 channels and none is a catch-up flag. There is no source of archive data anywhere for this subscriber, so this is not deferred, it is unbuildable. Reopen only if they change provider |
| M2-08 | First-class vertical bar layout | - | todo | The bar widget already renders in a vertical bar; this row is about making it good rather than making it work. Small, cosmetic, no blocker |

## Defects

Format: `<id> | severity | task | repro | state`. Severity is P1 to P4; P4
means diagnostic-only, visible to nobody but a maintainer reading a log.
State must NAME a state: open, fixed, verified fixed, accepted, superseded,
wont fix. `fixed` means a commit repairs it. `verified fixed` means QA re-ran
the original repro afterwards and said so, and the row cites where. A release
having shipped is not a state.

Ids are `D-` for a defect and `F-` for a finding. The distinction is tone, not
obligation, and `scripts/check-defect-ledger.py` treats them identically:
every id written into any tracked markdown file must have a row here carrying
a severity and a state, or `scripts/check.sh` is red (CLAUDE.md rule 13).

| ID | Sev | Task | Repro | State |
|---|---|---|---|---|
| D-LIVE-01 | P2 | M1.1-01 | TC-BRW-09/10, TC-RFR-10: with no query, All and any group over 200 channels show only 200 rows and the footer `First 200 of N - keep typing`; End/PgDn stop at row 199, channels 201+ unreachable by browsing, the `cached HH:MM - offline` footer never visible on such lists (UX 2.2 says All = every channel; R3 caps search results only). Evidence shots/run3-banner.png, harness/shots/run6-cap.png | verified fixed |
| D-LIVE-02 | P2 | M1.1-01 | TC-CFG-06: setting `epgUrl` from empty at runtime (the `omarchy bar set ... epgUrl` first-run path) never spawns the epg helper; `epg.pending` stays true, footer `Guide data loading...` until a manual `r`; non-empty -> non-empty changes fetch within 300 ms. Reproduced 4x (run6, run8a, run9, run9b), 4 s process watch shows no helper | verified fixed |
| D-LIVE-03 | P3 | M1.1-01 | TC-RFR-09: HTML body renders the raw helper sentence `source from 127.0.0.1 is not an M3U playlist (...) from 127.0.0.1 - check playlistUrl`; `Model.statusReason` knows `not_m3u`, the helper emits `not_a_playlist`. Expected UX 6.3 `Not an M3U file`. harness/shots/run2-notm3u.png | verified fixed |
| D-LIVE-04 | P3 | M1.1-01 | TC-FAV-08, SEC-14: `state.json` written by the Service FileView is mode 644 (ARCH: 0600; dir is 700, every helper file is 600). Names and ids only, no URLs | verified fixed |
| D-LIVE-05 | P3 | M1.1-01 | TC-RFR-01, TC-BAR-08: after `r` the footer holds `Refreshing...` for the whole 3 s transient then jumps to `N channels - updated HH:MM`; `Refreshed - N channels` (UX 6.1) never shows although the helper finished in ~40 ms and the notification fired | verified fixed |
| D-LIVE-06 | P3 | M1.1-01 | TC-BRW-21: `Ungrouped` appears at its first-seen position (between News and Padded on qa-groups.m3u) instead of last (UX 2.2). harness/shots/run1-open.png | verified fixed |
| D-LIVE-07 | P3 | M1.1-01 | TC-FAV-05: removing the last Recent entry hides the column entry but leaves the cursor scope on `recent` (`No channels in Recent`, `Recent - 0 channels`, no highlighted entry). shots/run1-recent-removed-card.png | verified fixed |
| D-LIVE-08 | P3 | M1.1-01 | TC-EPG-04: EPG fetch failure with an earlier window loaded sends the `Guide data error` notification but shows no `Guide data unavailable (...)` banner (`bannerKind` requires `!epgLoaded`). harness/shots/run5-epg-404.png | verified fixed |
| D-LIVE-09 | P3 | M1.1-01 | TC-UI-01: `r` in the not-configured state shows the `Refreshing...` transient for 3 s although nothing runs | verified fixed |
| D-LIVE-10 | P3 | M1.1-01 | TC-UI-02: with no cache, switching from a failed source to a new URL keeps the old source's error text (naming the old host) on screen with footer `Refreshing...` for the whole fetch instead of `Loading playlist... / Fetching from <new host>` | verified fixed |
| D-LIVE-11 | P3 | M1.1-01 | TC-CFG-02: timeout wording disagrees: helper default 20 s, guide reason `Timed out`, UX 6.3 `Timed out after 30 s`, README Limits `60 seconds` | verified fixed |
| D-LIVE-12 | P3 | M1.1-01 | TC-BAR-09: Enter/Space on the already-playing channel from another list (e.g. Favorites) keeps the previous `launchedFrom`, so the zap ring does not follow the list the user is on | verified fixed |
| D-LIVE-13 | P3 | M1-18 | TC-INST-05/10, TC-BAR-12, TC-UI-13: README lacks the `settings are lost on disable` note, lists two leftover dirs (runtime dir missing from Uninstall), no third-party-bar limitation, no qmllint warning baseline | verified fixed (docs) |
| D-LIVE-14 | P3 | M1-19 | TC-UI-13: qmllint reports 6 `signal-handler-parameters` and 1 `uncreatable-type` warnings on top of the recorded baseline categories (57 missing-property, 42 unqualified); 0 errors | verified fixed |
| D-LIVE-15 | P3 | M1.1-08 | PERF-06 run 2 / TC-PLAY-09: after the hung-mpv health-check sequence (SIGSTOP, two `mpv unresponsive` restarts, SIGCONT, stop) the next play logged `play failed: mpv did not answer` and opened no window for 6.6 s; the following play worked; plain stop -> play does not reproduce | verified fixed |
| D-LIVE-16 | P3 | M1.1-05 | TC-BRW-05/06 (UX 2.2): with a group column taller than the card (20+ groups), every reopen that lands on All starts the column scrolled one entry down, so `Favorites` is hidden above `All` until Left/h; first open and a reopen on Favorites are correct; a column that fits is correct. shots/run1-reopen.png, run2-reopen.png (QA-RESULTS R6) | verified fixed |
| D-LIVE-17 | P3 | M1.1-08 | TC-PLAY-06/09: once in 24 fresh launches (run5b) mpv mapped its window but never answered on the IPC socket; `stop`/`s` and the health check only send SIGTERM (Service.qml:421, :787), a wedged (or SIGSTOPped) mpv ignores it and is never reaped; in-flight control calls starved the health timer; `nowPlaying` moved on while the window stayed on the old channel. Not reproducible on demand (run5c 8/8 clean). Fix: escalate to SIGKILL after an ignored SIGTERM (QA-RESULTS R6) | verified fixed |
| D-LIVE-18 | P3 | M1.1-07 | README Limits says capped playlists show `a warning in the guide`; the guide renders no helper warning (run6, 50,500-entry list: `bannerKind none`, footer `50,000 channels - updated 01:53`; warnings only in playlist-status.json). Render them or reword the README (QA-RESULTS R6) | verified fixed |
| D-LIVE-19 | P3 | M1-09 | TC-CFG-12 / TC-UI-01: clearing `playlistUrl` at runtime (the `omarchy bar set ... playlistUrl ""` path) while a list is loaded flips `emptyKind` to `unconfigured` but the service keeps its channels, so the `No playlist configured` body and command box are drawn over the still-rendered channel list and group column (rows 10, showColumn true) until a new URL loads; survives close/reopen. Starting unconfigured is correct. Reproduced identically on 2ce0b52 (pre-existing, not a regression). shots/run5f-cleared.png, run5f-old-cleared.png (QA-RESULTS RR6) | verified fixed |
| D-SRC-01 | P3 | M2-01 | SRC-MIG-01: the migrated record keeps `fetchedAt 0` / `channelCount 0` (counts not copied from the migrated `playlist-status.json`), so the active row reads `not loaded yet` until the first refresh although the guide shows the migrated channels. QA-RESULTS.md S7 | verified fixed |
| D-SRC-02 | P2 | M2-01 | SRC-CLI-02/08: `omarchy bar set ... epgUrl` runs the EPG helper but the URL is never adopted into the history record; the next switch away and back writes `epgUrl (none)` into the settings and the EPG is silently lost (the guide edit path adopts it fine). QA-RESULTS.md S7 | verified fixed |
| D-SRC-03 | P2 | M2-01 | SRC-FR-11/CLI-03 (regression of TC-CFG-04): `omarchy bar set ... playlistUrl ftp://x` sets `settingsInvalid` but leaves the guide in `Loading playlist... / Fetching from x` for good instead of the error state (no helper runs). `shots/sources-cli-ftp.png` | verified fixed |
| D-SRC-04 | P2 | M2-01 | SRC-SW-06: Enter on a never-fetched source whose probe fails shows the `Switched to <label>` transient before the probe and then no error at all (`sourceErrors` only); the previous source silently stays active. `shots/sources-never-switch-failed.png` | verified fixed |
| D-SRC-05 | P3 | M2-01 | SRC-RM-03: when the active source is removed and the guide returns to setup, the stale search query stays as the header above the first-run form instead of `Search channels...`. `shots/sources-none-active.png` | verified fixed |
| D-SRC-06 | P3 | M2-01 | SRC-FR-11/CLI-03/UI-14: CLI-invalid values render the M1 reasons with a host (`Unsupported URL from unknown source`, `URL too long from h.test`) instead of the UX 5.4 messages; the error-state hint says `r reload` where UX 5.3 says `r retry`; a U+2028 inside an `http://` URL gets the `scheme` message | verified fixed |
| D-SRC-07 | P3 | M2-01 | SRC-HELP-07: helper `cache prune` deletes the `--active` key's directory when it is not also listed under `--keep` (Service.qml always lists every key, so no user impact today; hardening) | verified fixed |
| D-SRC-08 | P3 | M2-01 | SRC-XT-02: an Xtream server URL with a `#fragment` (`http://h.test/#frag`) is accepted, the fragment dropped silently, where ARCH 3.4 says `bad_server` / UX 5.4 `server_path` | verified fixed |
| D-SRC-09 | P3 | M2-01 | SRC-SEC-21: `parseState` / `normalize_state` keep a history record whose URL contains a NUL (control character) instead of dropping or sanitizing it | verified fixed |
| D-SRC-10 | P3 | M2-01 | SRC-H13: `omarchy bar set ... playlistUrl <new>` while another source is active attaches the settings' current `epgUrl` (the previous source's, Xtream credentials included) to the new CLI record (`editMasked` shows it, row shows `EPG`) | verified fixed |
| D-LIVE-20 | P1 | M2-01 | SRC-SW-01/02/06 + add, live shell only: `Service.qml persistActive` writes the new active source through `shell.updateEntryInline` and `shell.json` is updated correctly, but the host never re-emits the plugin's `settings` for its own write, so `root.playlistUrl` stays stale and `reconcile()` never runs. Enter on a source row shows `Switched to <label> - N channels` while the guide keeps rendering the previous source (20+ s, no timeout, no error); a successful add never becomes active (UX-SOURCES 1.5); a second Enter reports `Could not save settings - try omarchy bar set` because `shell.qml` takes its `if (!dirty) return false` branch for a value already persisted. Recovers only on an external write (`omarchy bar set`, even of the identical value) or `omarchy restart shell`. Invisible to the dev harness, which supplies a fake `updateEntryInline` (QA-SOURCES section 6). `shots/E-back-t1.png`, `qs log`: `switch did not observe a cache load within 5000 ms` (QA-RESULTS L2, L6). Fixed at `845d445`: the service applies its own settings write locally (`Model.settingsWithOwnWrite` over `hostSettings`) and drops the override as soon as the host reports anything else; a `false` return from a writable entry is the host's `!dirty` branch and counts as success (`Model.barEntryWritable` separates a real persist failure). QA re-ran the original repro on the live shell: add is active in 723 ms, four Enter switches arrive in 145-161 ms with the guide, footer and `shell.json` all agreeing, and the second and third Enter on the same row report nothing (QA-RESULTS P2) | verified fixed |
| D-LIVE-21 | P1 | M2-01 | SRC-RM-01/03/05, live shell only: removing the active source clears `playlistUrl` in `shell.json` and sets `activeSource null`, but the guide never returns to setup - it sits on `Loading playlist... / Fetching from <the removed source's host>` with `configured true` / `status loading` for 18+ s, waiting on a fetch whose cache dir the removal just deleted. Same root cause as D-LIVE-20 (the cleared value is the plugin's own write); the live recurrence of the D-SRC-03 / TC-CFG-04 stuck-loading family through the Sources screen. `shots/I-return-to-setup.png`, `shots/I-stuck-loading.png` (QA-RESULTS L6). Fixed at `845d445` with D-LIVE-20 (same root cause): `configured` goes false 821 ms after the confirm, `status ready`, `channels 0`, and Esc lands on the first-run surface with the `Saved sources (1)` link (QA-RESULTS P3) | verified fixed |
| D-LIVE-22 | P3 | M1.2 | QA-RESULTS P10/O18, found 2026-09-13 at 845d445 (pre-dates the P1 fix; introduced by `cf22c3f`, the cosmetics lane): when the EPG helper reports a warning **and** the playlist is serving a stale cached copy after a failed refresh, the footer shows `Guide data warning: 1 programmes for channels not in the playlist dropped` instead of `8 channels - cached 22:49 - offline`. `Model.footerStatus` ranks `warning` above the count line and the `cached HH:MM - offline` marker lives in that count line, so it is the footer's only staleness cue that the warning displaces. This matches the letter of the UX.md precedence list (warning > plain channel count) and the failure itself stays visible in the banner (`Playlist refresh failed (Connection refused) - showing cached copy from 22:49 - r retry`, `bannerKind playlistError`), so it is filed as cosmetic, not as a hidden failure. Repro: `d19.sh` / `h21` (`logs/d19.txt`) | **verified fixed** by the automatic gate; no screen is owed, because the whole behavioural change is in `Model.js` -- `git show e5ac497 -- Guide.qml` is comment-only, so the fix is the `footerDegraded` rung in `Model.footerStatus` and nothing else. Two suites call that shipping function for real: tests/Model.test.js:2045-2110 (the ladder as a table, walked pairwise and all-at-once, 54 checks, plus 7 named D-LIVE-22 checks) and tests/Model.spec.qml `test_footerPrecedenceCachedOverWarnings` in the Qt engine; both are floored with zero margin in scripts/check.sh (NODE_CHECKS_MIN 1427, QML_SPEC_MIN 68). Re-proven red 2026-09-20 by deleting the one line `if (footerDegraded(o)) return footerCounts(o)`, which is the pre-fix ladder byte for byte: node 1,427 checks / **9 failures**, matching e5ac497's own recorded count, and qml spec 67 passed / **1 failed**. The one join no test reaches -- Guide.qml `stale: root.serviceStatus === "cached"` -- was confirmed on a real screen by the defect report itself: QA-RESULTS O18 recorded `bannerKind` `playlistError`, which Guide.qml emits only when that same expression is true. So the input was right live and only the ranking was wrong. The four live passes that skipped this repro did not need to run it. |
| D-PLY-1 | P1 | M2-02 | PLY-LIFE-12 / PLY-RST-06 / PLY-SOCK-01, live shell, found 2026-09-14 at 8f9447e: after any helper-driven ladder-and-respawn the shell goes idle while the new player keeps playing. Two independent triggers, both deterministic: (a) `kill -STOP` the player and wait ~21 s for the health check's two-strike verdict (journal: `omarchy-iptv: mpv unresponsive, restarting player`), 3/3; (b) `rm "$XDG_RUNTIME_DIR/omarchy-iptv/mpv.sock"` then play another channel, 2/2. The relaunched mpv is genuinely playing (`idle-active` false, real `path`, window mapped and titled, its `user-data/omarchy-iptv` stash reading `playing:true` with the right id) while `status` reports `playing false`, `nowPlaying null`, `player.up/attached/wanted` all false. Because `wanted` is false the 250 ms retry timer is off, so it never reattaches (unchanged at 15 s and 30 s). Exactly one player exists throughout, so this is not a second-window defect. `stop` still reaches it and a shell restart reattaches to the same pid and restores the right state - but the interface already says stopped, so the user has no reason to try. The plan's P1 list verbatim: "the interface goes idle while the player keeps playing". Suspected: the `player restart --from term` reply path in `Service.qml` not re-arming `playerWanted` after a relaunch it issued itself. QA-RESULTS.md M8. **verified fixed at b16b479** (QA-RESULTS "M2-02 fix verification on b16b479" V2): neither trigger reproduces - A 3/3, B 2/2, shell stays `playing`/`attached`/`wanted` with one player and one window, unchanged at t+30 s. QA also counted the journal's relaunch line directly and found **exactly one relaunch per wedged player, never two**, because the harness check for that clause never runs (D-PLY-9) | verified fixed |
| D-PLY-2 | P2 | M2-02 | PLY-WEAK-02, live shell, found 2026-09-14 at 8f9447e: `omarchy plugin remove io.github.rmcdavid.iptv --yes` with a channel playing leaves the player alive and windowed at 21 s and still at 45 s, where `disable` stops it in 6676 ms. `remove` deletes the plugin directory, and `Component.onDestruction`'s orphan-check is `python3 <plugindir>/bin/omarchy-iptv player orphan-check`, so there is nothing left to exec. The README's escape hatch lives in the same deleted directory and cannot be run either - the sentence naming it is in the bullet that names removal. Restoring the directory from a backup made the command work and it reaped the orphan at once. PO-2's second named acceptance case, with both halves of its compensating control failing together. P2 by the letter (logging out remains documented); **the lead may reasonably raise it to P1**. QA-RESULTS.md M8. **closed as accepted under PO-8**, not fixed. The stranding reproduces (alive and windowed at t+32 s, plugin directory gone) and is accepted. The documented recovery was tested verbatim at b16b479: `disable` first stops the player in **6801 ms**; for a stranded one the README's `pgrep -af -- '^mpv .*--wayland-app-id=omarchy-iptv'` returns exactly one line and its `pkill` reaped it in **145 ms**. The anchor warning is literally true - without the leading `^mpv ` the same pattern also matches the shell pasting it (3 matches, QA's own `$$` among them) | closed (accepted, PO-8) |
| D-PLY-3 | P2 | M2-02 | PLY-WEAK-06 step 6 / PLY-FIX-01, live shell, found 2026-09-14 at 8f9447e, 2 of 3 runs: the PO-3 mark lands correctly and silently, but `state.json.session` is still on disk afterwards instead of cleared. The next shell start consumes the record again and re-marks the same channel with the same `HH:MM`; a third start is clean. This is the second-verdict-on-an-event-already-dealt-with the fix lane `c458ebf` says it closed, surviving in the deferred drain path (`applyUserState` -> `markDeadSession`) rather than in the twelve branches that lane routed. Suspected the write racing `stateFile`'s own load (same handler as D-PLY-4), or `saveState()`'s `dirsReady` deferral. QA-RESULTS.md M8. **verified fixed at b16b479**: the PLY-WEAK-06 procedure run end to end 3 times, `jq .session state.json` is `null` every time, `state.json` still `0600`, **0** notifications, and the next start re-marks nothing. Was 2 of 3 failing; now 0 of 3 | verified fixed |
| D-PLY-4 | P2 | M2-02 | ARCH-P section 15's known startup race, gated and measured on 2026-09-14 at 8f9447e: **it reproduces, 14 losses in 30 trials (47%)**. Restart the shell and issue `play` from the earliest instant the successor's IPC accepts one (landed on the 3rd or 4th attempt in all 30 trials); the arriving state file then replaces `userState` wholesale and the session record the play just wrote is gone. Every loss reads `playing true`, `nowPlaying` correct, window mapped, `session` null. Consequence proven end to end: after a lost race the stream was killed with no shell attached and the reattaching shell marked nothing (`failedAt {}`) and raised no toast, so PO-3 is silently unavailable for roughly half the plays issued at shell start. Not fixed by QA on the lead's instruction; the "fix only if it reproduces" gate is met. QA-RESULTS.md M5. **verified fixed at b16b479**, re-measured with QA's own `race-trial.sh` from the 8f9447e pass (diffed: logic identical): **30 trials, 30 WON, 0 LOST, 0 VOID**, against 14 lost in 30 before. The play landed on attempt 3 in 29 trials and 4 in one, the same narrow window as the losing pass | verified fixed |
| D-PLY-5 | P2 | M2-02 | PLY-SEC-14 / PO-5, found 2026-09-14 at 8f9447e: PO-5's required README sentence naming the `--ytdl` cost is absent - `ytdl`, `yt-dlp` and `youtube` appear 0 times in `README.md` and 0 times in `CHANGELOG.md`. The cost is real and was reproduced live: with the documented setting `mpvArgs '--ytdl=yes'` and a stream that fails to open, mpv's `ytdl_hook` spawned `/usr/bin/python /usr/bin/yt-dlp --no-warnings -J --flat-playlist ... -- http://qa-user:qa-secret@127.0.0.1:9/live/qa-token-XYZ/dead.ts`, putting the full credentialed URL on another process's 0444 command line for seconds (99 sweep samples caught it). The default `--ytdl=no` is clean. The README's headline claim "No stream address, credential or header value ever reaches any command line" therefore has an unstated exception the README itself documents how to enable. Suggested: one sentence beside the `mpvArgs` row and in the 0.3.0 `Known limitations`. QA-RESULTS.md M8. **verified fixed at b16b479**: `status.warnings` carries `Player warning: mpvArg --ytdl hands the stream address to another program` and the guide renders it on its existing single warning line (screenshot). The option is **not** refused - `--ytdl=yes` is last on mpv's argv and mpv's `ytdl` property reads `true`. The cost is still real (yt-dlp with the full credentialed URL, 206 samples) and is now named in README beside the `mpvArgs` row and in CHANGELOG 0.3.0 | verified fixed |
| D-PLY-6 | P3 | M2-02 | PLY-PERF-02 / PLY-STOP-03, found 2026-09-14 at 8f9447e: a wedged (SIGSTOPped) player takes ~6.27 s to reap, not the budgeted 4.5 s - 6258 / 6265 / 6269 / 6276 ms across four runs, deterministic. The ladder is quit@0, TERM@2.0, KILL@4.0, settle@4.5, but against an unresponsive player the quit rung first spends the full `--ipc-timeout` 2.0 s, which the budget does not account for. The player is reaped and the interface clears in 52-59 ms. README says "within about four seconds". Either move the budget and the prose to about six and a half seconds, or skip the quit wait when the probe already said `responsive:false`. QA-RESULTS.md M8. **verified fixed at b16b479**: wedged reap **4236-4263 ms** over five live runs against the 4500 ms budget. QA's controlled A/B on an identical rig, only the helper swapped, gives **6187 ms at 8f9447e vs 4177 ms at b16b479** - exactly the ~2.0 s `--ipc-timeout` the defect named. Responsive path unchanged (274-305 ms, no SIGKILL) | verified fixed |
| D-PLY-7 | P3 | M2-02 | PLY-SEC-07, found 2026-09-14 at 8f9447e: the player can write a durable file outside the documented list, into `$HOME`. mpv inherits the shell's working directory (`/home/ricky`) and its default key bindings are live on the focused window, so `s` on the player writes `~/mpv-shot0001.jpg` (mode 0644) - a durable image of what was being watched, in a directory README `Files it writes` does not mention. The milestone already reserves `--screenshot-template` against durable exposure; the default template is not covered. Suggested: spawn with `--screenshot-dir` under the runtime directory, or name it in `Files it writes`. QA-RESULTS.md M8. **verified fixed at b16b479**: the player's cwd is `/run/user/1000/omarchy-iptv`, and its argv carries `--screenshot-dir` and `--watch-later-dir`. `s` writes `~/.local/state/omarchy-iptv/screenshots/mpv-shot000N.jpg` at **mode 600** (dir 700); `Shift+Q` writes `$XDG_RUNTIME_DIR/omarchy-iptv/watch-later/<md5>` at **mode 600** (dir 700). **Nothing lands in `$HOME`** - no new top-level entry across the whole pass and no `mpv-shot*` anywhere outside the documented directory | verified fixed |
| D-PLY-8 | P3 | M2-02 | PLY-STOP-03, live shell, found 2026-09-14 at b16b479, **pre-existing**: a wedged stop reaps the player at ~4.25 s but leaves `$XDG_RUNTIME_DIR/omarchy-iptv/mpv.sock` behind - measured still present at t+66 s with the shell idle. PLY-STOP-03 says the socket is unlinked once the final connect is refused, and `connect()` does return `ECONNREFUSED`, so `settle_socket()`'s guard should fire; the real guard is `socket_is_dead()`: a killed player clears its command line in about 0.2 ms, so `find_player()` goes blind FIRST, while its listening socket stays bound for another 2.7 to 4.9 ms. The single-shot guard simply looked too early. Measured on real mpv, both orders confirmed; the quit path reverses them, which is why it always unlinked. The responsive stop path unlinks correctly (5/5). **Not a regression**: QA's A/B on an identical rig leaves it behind at both 8f9447e and b16b479, so the prior pass recorded PLY-STOP-03 as pass without asserting the clause. Harmless - the residue is `600` inside a `700` directory, `player probe` reports `running:false` through it and removes it, and the next play succeeded in 665 ms. Either implement the clause or drop it from the plan. QA-RESULTS.md V9 | verified fixed, merge 245a25c. Confirmed live twice on real windowed mpv: the socket is gone at t+0.2 s and still gone at t+1 s, t+5 s and t+30 s in 10 of 10 stops (5 wedged, 5 responsive) at a939fd7 (QA-RESULTS D2) and again 10 of 10 at d76b649 (QA-RESULTS L5), with no probe/status/play of the tester's own in between and the `the player outlived a stop` backstop line absent in all twenty; it failed 5/5 at both 8f9447e and b16b479. PLY-STOP-03 was rewritten from a mechanism description into an assertion with a deadline and that control (docs/QA-PLAYER.md:163). Gated: tests/test_player.py SettleTest drives helper.settle_socket on an injected clock and the real `player stop` verb against a process double that sheds its command line while still listening. CORRECTED per finding F3 and CL12: a windowed player unbinds 9.6 to 24.7 ms after a kill, not 15 to 37; teardown proportionality is DISPUTED between two measurements and must not be recorded as fact (CL12). settle_socket, socket_is_dead, settle_under_lock and player_stop are byte-identical at HEAD to the tree the live confirmation ran on. |
| D-PLY-9 | P3 | M2-02 | test tooling, found 2026-09-14 at b16b479: `scripts/dev-harness/player-scenario.sh` silently drops its own most important D-PLY-1 check. `log_count()` (line 149) is `grep -acE ... \|\| echo 0`, and `grep -c` prints `0` **and** exits 1 on no match, so the function returns two lines; line 374's `$(( $(log_count ...) - before11 ))` then dies with `arithmetic syntax error (error token is "0")`. The check that dies is **"P11 exactly ONE relaunch, not a second one at the healthy player"** - the assertion for the half of the D-PLY-1 fix that cancels the delivered relaunch's timer. It aborts at both b16b479 and the 396a69a baseline, so the 75/0 (now 83; the repaired check plus positive controls) and 60/20 summaries are each one check short. QA measured the property by hand instead (delta exactly 1, 3/3 - but SUPERSEDED as evidence: that journal string is emitted by both the fixed and the unfixed tree, so counting it proves nothing. The property is now asserted on an intent counter that reads 1 here and 2 on the unfixed tree (merge b32833a)). Fix: `\|\| true` with `head -1`, or `\| wc -l`. QA-RESULTS.md V9 | verified fixed, merges 7468a08, 739c229 and b32833a. Proven live and proven in the gate. Live at d76b649 (QA-RESULTS L3): player-scenario.sh reports 85 passed / 0 failed / 85 executed, against 68 / 22 / 85 at the 396a69a baseline, and the repointed assertion discriminates - `PASS P11 exactly ONE relaunch` here, `FAIL (got '2', want '1')` there - where wave two (QA-RESULTS D4) found that same line passing on BOTH trees because it counted a journal string only the fixed tree emits (CL10). In the gate on every commit: scripts/check.sh runs scripts/qa-lib-test.sh (155 checks, green), which sources the shipping scripts/qa-lib.sh, reproduces the shipped `grep -acE` form with its zero fallback verbatim and proves it moved no counter at all, proves qa_count makes the same statement execute, drives the four P11 lines extracted verbatim from player-scenario.sh (PASS at 1, FAIL at 2, FAIL on NOFIELD), and asserts the scenario's declared floor equals the assertions it actually has. player-scenario.sh is unchanged since the confirming run and qa_count is unchanged. F2, the same class in qa-player-scenarios.sh, is closed at line 693. |
| D-PLY-10 | P3 | M2-02 | PLY-SEC-07 / README `Files it writes`, found 2026-09-14 at b16b479: the player writes mpv's shader cache into `~/.cache/mpv/` (two files, mode `600`, during one containment cycle), outside the documented list. The containment fix gave the player a working directory and redirected screenshots and resume positions, but mpv still inherits `HOME`/`XDG_CACHE_HOME`. **Privacy impact none** - no needle, no URL, nothing keyed to content, `600`, and the directory predates the plugin and is shared with any other mpv use. A completeness gap in `Files it writes`. Suggested: one line there, or `--no-config` plus a cache redirect if total containment is the intent. QA-RESULTS.md V9 | verified fixed, merge 245a25c, both halves - and the documentation half had INVERTED before anyone looked. Containment confirmed live: the redirected directory is 0700 with 26 files at 0600 and the user's shared cache was untouched across about 120 player starts. But the README still listed `~/.cache/mpv/` under Files it writes, which the fix had made untrue in the opposite direction - claiming a write that no longer happens while omitting the two that do. Corrected at README.md:300 by reading the argv the helper really builds rather than its docstring: `mpv_launch_argv` emits --gpu-shader-cache-dir and --icc-cache-dir into $XDG_RUNTIME_DIR/omarchy-iptv/shader-cache and --watch-later-dir into .../watch-later, so nothing durable lands in $HOME; --screenshot-dir stays under the documented state directory. Stated as a default and not a guarantee, since neither path is reserved (ruling CL2) and an mpvArgs token can still move them. |
| D-PLY-11 | P3 | M2-02 | TC-PLAY-10, found 2026-09-14 at b16b479, **10 of 20 cold concurrent bursts, observed mechanism**: eight truly-concurrent `play` calls left `nowPlaying` naming `t:qa.live` while the window title and mpv's `media-title` both read `QA Plain No Headers`, and it did not self-correct at t+5/20/40 s. The plan's own P2 shape, filed P3 because it is barely reachable: **1 of 8** parallel bursts, **0 of 6** targeted re-attempts, **0 of 4** with the same zaps issued serially - which is all the guide can produce, being single-threaded QML. One player and one window at all 80 samples in every run, so it is a state-reporting divergence, not a two-player defect. Worth a look at the last-intent-wins path under simultaneous IPC. QA-RESULTS.md V9 | verified fixed, merge 739c229 (the helper stand-down) with the CL5 shell-side re-apply. The live confirmation this row was awaiting arrived at d76b649 (QA-RESULTS L1, 2026-09-14): same instrument, same counts, **0 divergences of any kind in 20 cold concurrent bursts** where wave two measured 10 of 20 at a939fd7 - all twenty verified genuinely cold, nowPlaying.id == stash.id 20/20 at t+6 s, shell name == media-title 20/20 and == window title 20/20, one player and one window at every sample, controls unchanged at 0 of 10 warm and 0 of 6 cold-serial. Both halves witnessed in the journal: the helper stood down 5 times and the shell re-applied its own intent 12 times. CL6's invariant now has an outcome test and passes 4/4 - convergence in 4.48-4.53 s against a 10 s tick, with nowPlaying never once relabelled across 120 samples (QA-RESULTS L2). Recorded honestly and not counted: the rig's shakedown run diverged at t+6 s and converged by t+20 s (finding F5). Ordering T-A (an adopting start re-applying its own channel) stays characterised at the helper rather than fixed, and is repaired shell-side by sessionIntentRepair. Gated: tests/test_player.py OrderingTest drives the real `player start` and `play` verbs with the handshake gated, and Model.playFork / reconcileVerdict / sessionIntentRepair are called for real from tests/Model.test.js and tests/Model.spec.qml. All of those, plus Service.qml play()/handlePlayerResult()/checkPlayerChannel(), are byte-identical at HEAD to the confirmed tree; only the D-CL-1 log wording changed. Severity stays P3 on reachability per CL11. |
| F-CHNO-3 | P3 | M2-03 | coverage, found 2026-09-14: `docs/M2-03-CHANNEL-NUMBERS.md` section 10.6 specified four harness verbs (`number`, `numberState`, `commitNumber`, `cancelNumber` on `IpcHandler { target: "harness" }`) plus `numberEntry` / `hasNumbers` on `state()`, and none of them existed in `scripts/dev-harness/shell.qml`, so scenarios N1-N16 had a specification and no runner | fixed at 3c2b87f (CN23): all four verbs present (`shell.qml:518, 531, 535, 543`) and `state()` returns both fields (`shell.qml:681-683`); `scripts/check.sh` runs the chno-entry preflight with a floor of 21 |
| F-CHNO-4 | P3 | M2-03 | process, found 2026-09-14: the board still recorded M2-03 as `todo` with no owner and no evidence although the work had merged at `cc1d73e`. Filed as a one-off staleness; it was not one | verified fixed, terminal-only, 2026-09-20. Instance: `docs/STATUS.md:94` now carries M2-03 as `done` with owner `ARCH -> FE -> QA` and the merge and release evidence, so the stale row itself is gone. Class: `scripts/check-defect-ledger.py` runs as step 8 of `scripts/check.sh` (`check.sh:405`) and is green on the live board -- 92 rows, 92 ids, 34 tracked markdown files, exit 0. Re-proved by mutation rather than by reading (CLAUDE.md rule 11): deleting THIS row from a git copy of the tree takes the board to 91 rows and the checker exits 1 naming F-CHNO-4 as filed in prose with no row. `tests/test_defect_ledger.py:26-30` loads the shipping script through `importlib` and calls it for real; its 17 cases cover the missing row, the orphan row, the empty state cell, a cell naming no state, a missing severity, a truncated row and a duplicate id. Stated so the close is not over-read: by its own docstring the checker does not check that a state is TRUE, and it does not catch this defect's ORIGINAL shape -- a milestone row still reading `todo` after the work merged -- which was fixed by hand and stays a human matter. Nothing here needs a screen or a playlist. |
| D-RUNG-1 | P3 | M2-09 / accessibility | The guide's faintest opacity rung, 0.45, is under the 4.5:1 contrast threshold in **23 of 23 installed themes**, worst 1.98:1, median 3.17:1. It carried the footer status line, the footer's verbs, the group entry count and the Sources pinned count. The finding survives the known error in the contrast model: that model reads about 1.25 ratio points LOW against the one rendered measurement this project has taken (QA-RESULTS.md:4720-4732), and 3.17 plus 1.25 is still under the line | **fixed** under PO ruling SG2: all four sites raised to the 0.7 key rung, taking 23 of 23 under to 6 of 23 under. Accepted cost, recorded in docs/UX.md:756-757: the key/verb pair collapses to one rung so key names no longer stand out. The residual 6 themes are a known limitation joined to D-RUNG-2. **Not yet seen on a real screen** |
| D-RUNG-2 | P3 | M2-09 / accessibility | The 0.52 rung, which carries the detail line, the header scope label and the right-hand meta, computes under 4.5:1 in 20 of 23 themes | **open, and the obvious fix was REFUSED with numbers.** A per-theme contrast floor would raise it, but it would spend the guide's only working hierarchy channel: the name-versus-secondary separation on a non-cursor row drops from a median of 2.86:1 to 2.24:1, and to 1.35:1 in the worst theme. It would pay that on a model that DISAGREES with the only rendered measurement this project has, in the direction that says the screen is already fine (model 3.54, screen 4.79 and 7.13, twice). After the floor all 23 themes land at exactly 4.50, no margin, while the proposed test asserted 4.50 - green on every layer with the screen still under threshold and the hierarchy already spent. **Calibration DONE 2026-09-15, and it withdrew one of the three reasons this was refused** (docs/UX-GUIDE-AT-SCALE.md section 16). Measured off real screenshots on a dark theme and a light one, at two opacities: the model is accurate to within **0.14** ratio points, not 1.25 out. The 1.25 came from an earlier pass measuring the failure GLYPH at opacity 0.8 and comparing it against a value computed for the 0.52 text rung; 0.8 computes to 7.13 on retropc, which is exactly what that pass reported as its peak. Two measurements of different things, read as an error in the model. Pinned by `tests/fixtures/contrast-calibration.json` and 5 checks, one of which fails if the tolerance is widened to hide a bad model. **So the finding is real and this is no longer blocked on measurement.** Two reasons still stand and one got STRONGER: every sample came in BELOW its prediction, so the model is optimistic and a design computing exactly 4.50 renders near 4.40, under the line, with every test green. Back for redesign with two binding constraints: target above 4.5 the way the bar glyph got a 4.71 floor, and state the hierarchy cost AT that higher target, since raising the target makes the separation loss worse |
| D-RUNG-3 | P3 | M1-11 bar widget | **The one element of this plugin that is on screen permanently**, and the contrast design missed it entirely because it grepped for `opacity:` and this dims by a different mechanism: `BarWidget.qml:66` is `Qt.darker(root.barFg, 1.55)` for the idle glyph, named as the intended behaviour at docs/UX.md:663-664. Under 4.5:1 in **7 of 23 installed themes** | **superseded by D-RUNG-5's fix, and this cell described code that no longer exists.** It named `Model.BAR_IDLE_ALPHA` at factor 1.25 with a floor of 4.71; the shipping code is `BarWidget.qml:72`, `Util.alpha(root.barFg, Model.BAR_IDLE_ALPHA)` at 0.86, and that symbol resolves in no source file. A reader sizing the contrast work read a factor, a symbol and a floor that were all gone. Corrected in phase 0, and `scripts/check-defect-ledger.py` now fails on any backticked `Model.X` in tracked markdown that does not resolve, so this class of drift is a red gate rather than a careful reader. **Its threshold claim stays UNASSERTED until D-RUNG-6 settles**, because that claim was measured against the menu fixture and the bar's real surface is not established. The ordering claim (idle dimmer than active) is what survives and is what the tests assert. The cost: the active glyph now reads about 1.57x the contrast of the idle one instead of 2.32x. Affordable here and nowhere else in the plugin, because ruling R7 already gives every state its own glyph, so the dimming is decorative and state is never carried by colour. 4 node checks, all 3 mutants caught including a factor that lands exactly on 4.5. **Not yet seen on a real screen** |
| D-RUNG-4 | P3 | M2-09 | The channel name and number on the CURSOR row are inked with the theme accent, which is under 4.5:1 against its own fill in 8 of 23 themes even at full opacity. No opacity change can reach it; only changing the ink can, at the cost of the cursor row losing its accent | **fixed** under PO ruling SG4. `Model.cursorInk` keeps the accent wherever it already clears 4.70 against its own composited fill (15 of 23 themes, byte-identical on screen) and elsewhere mixes it the least distance toward the menu text that does. The eight mix fractions are asserted so the COST is a number: white 0.07, lupine 0.11, solitude 0.15, osaka-jade 0.18, nord 0.30, catppuccin-latte 0.35, miasma 0.42, rose-pine 0.70. Floor 4.7025. The target is 4.70 rather than 4.50 because the rose-pine capture measured this arithmetic optimistic by 1 per cent at title size and 2 at body size, so it renders near 4.62; **the same target would not be safe for caption-size text**, which lost 7 to 9 per cent. **Two amendments the reviewer forced.** The cursor row's channel number went to full opacity, not the shared ink alone: at its old 0.8 rung it stayed under 4.5:1 in 16 of 23 themes even with the corrected ink, floor 3.25, and it is the one thing a user stares at during numeric zap. And a FIFTH site was found that the design's own gate reported clean over, `selectedText: root.selectedText` handed to the host's ConfirmDialog, whose selected button draws that colour over that same fill one keypress from the remove-source dialog; the guard now counts both spellings. **Plus the addition:** a left rule in `cursorInk`, drawn outside the fill so it lies on the card. Until now the accent ink was the ONLY thing saying which row is selected, because the selected fill measures 1.12-1.23:1 against the row fill in 23 of 23 themes and there is no border. Correcting the ink alone would have spent that signal to buy legibility. 9 node checks; 5 of 6 mutants caught first time, and the sixth, reverting the number's rung, was uncovered until a check was added for it. **SEEN ON A REAL SCREEN, rose-pine, 2026-09-15.** The defect reproduced almost exactly as modelled: the cursor row's name measured **2.78:1** where the model said 2.80. After the change it measures **5.83:1**, and the new left rule renders at 5.98:1 against the card. One honest discrepancy, recorded rather than smoothed over: the rendered ink is `#59537e`, nearer the menu text than the 0.69 mix `#576685` the model computes, so the screen came out 1.1 ratio points BETTER than the 4.72 target. The direction is safe and the defect is fixed, but the model and the runtime disagree about which ink was chosen and I do not know why. Given D-RUNG-6 was exactly this class of gap, it is filed as D-RUNG-7 rather than treated as a happy accident |
| D-RUNG-5 | P3 | M1-11 bar widget | **On a light theme the idle bar glyph renders BOLDER than the active one**, which is the opposite of what docs/UX.md:663-664 promises. `Qt.darker` reduces the ink's HSV value, so against a pale background it raises contrast rather than lowering it: measured at the shipped factor, catppuccin-latte 11.00 idle against 7.06 active, rose-pine 10.76 against 6.66, plus flexoki-light and lupine. On the `white` theme the two are identical at 21.00, because darkening pure black changes nothing | **fixed at 62e7d39, not live-confirmed.** The bar dims by alpha now rather than by `Qt.darker`, so the idle glyph is dimmer than the active one in all 23 themes, dark and light alike, asserted by two node checks. It was pre-existing and **narrowed** by the earlier fix rather than caused by it (rose-pine idle 10.76 to 8.69). Found because a test asserting "dimmer in every theme" went red, which is the assertion being right about the world and wrong about the code. No state is lost: ruling R7 gives each state its own glyph, so the bar never relies on colour. Fixing it properly means dimming toward the BACKGROUND rather than toward black, which is a different operation from `Qt.darker` and belongs with the D-RUNG-2 calibration |
| D-A11Y-2 | P2 | a11y harness | `fidelity.guard_tree()` is documented as the one call a harness gates itself on, and it ran 4 of its 8 layers: `check_kit` (L8) and `ungraded_surfaces` (L7) were reachable from the command line alone. Measured by the reviewer: swapping the host kit underneath the generated copy turned a real credential failure GREEN with `guard_tree` returning no findings. The guide's form fields ARE host components, so the credential the harness hunts is published by `Ui/TextField.qml`'s own accessibility, not by anything Guide.qml declares | **fixed** the same day it was filed. `guard_tree` now runs all four, the kit must be graded unless the caller passes `kit=False` on purpose, and L7 has an allow-list (`UNGRADED_ACCEPTED`) so it reports drift instead of restating a known state. Three new tests pin it, one asserting by inspection that the entry point calls every layer it has. A thing named as the complete check that was not the complete check: CLAUDE.md rule 13, inside the guard written to stop rule 13 drift |
| D-A11Y-3 | P2 | a11y harness | 32 python tests landed in `tests/a11y/` and **`scripts/check.sh` could not see a single one of them**. `python3 -m unittest discover -s tests` does not recurse into a directory that is not a package, so the suite count stayed at exactly the 382 it was before the tests existed and the gate stayed green. The fourth silent no-op this project has found, and the first to land in pushed code | **verified fixed** by the automatic gate. scripts/check.sh carries an explicit `a11y fidelity guard` step running `python3 -m unittest discover -s tests/a11y` with its own floor, `A11Y_TESTS_MIN=32`, plus a guard for a run that prints no count at all. It discovers 32 and is green, while `discover -s tests` separately runs 423 - the two sets are disjoint, which is the defect's own mechanism made visible instead of silent. PO ruling stands: the FIDELITY half is in the gate, the AT-SPI half stays out, because this file is pure text, needs no display, D-Bus or Qt, runs in about 8 s and is green on shipping code. Re-proven 2026-09-20 by mutation: breaking one case in tests/a11y/test_fidelity.py makes the step exit non-zero, so check.sh reports FAIL rather than passing over it. One residual, new and not this row: nothing asserts that the check.sh STEP itself still exists, so deleting the step would still be silent. |
| D-A11Y-4 | P3 | CLAUDE.md rule 5 | Rule 5's second half said any annotated item publishes its `text` to the accessibility bus. **Measured on Qt 6.11.2 with a probe on the real bus and it is wrong**: `Accessible.StaticText` publishes its `Accessible.name` as the AT-SPI text body and its own `text` property never reaches the bus; `Accessible.Button` exposes no text interface at all; only `Accessible.EditableText` publishes its `text` | **fixed.** The rule now says what was measured, which NARROWS the sink to elements declared editable and explains why the D-A11Y-1 exposure is confined to the form fields and the search line at `Guide.qml:2088`. An over-broad security rule gets ignored rather than followed, so being wrong in the safe direction is still being wrong |
| D-A11Y-5 | P3 | docs/UX.md 7.1 | The channel row's `row N of M` announcement was marked **UNVERIFIED, "never promised here and never delivered"**, and both clauses were false. It is promised at docs/UX-GUIDE-AT-SCALE.md:768 and :865 under ruling D5, delivered at Model.js:4213-4215, wired at Guide.qml:2452 and asserted in the node suite. A harness built strictly to that table would have asserted the absence of a string the shipping code produces | **verified fixed**, with the citations repaired. docs/UX.md 7.1 now reads PROMISED AND DELIVERED for the channel row's `, row N of M`. Re-checked 2026-09-20 by CALLING the shipping function rather than reading the table (CLAUDE.md rule 14): `Model.rowAccessibleName({name: 'Channel 7', rowIndex: 7, rowCount: 10000})` returns `Channel 7, row 8 of 10,000`, exactly what the table claims. Promised at docs/UX-GUIDE-AT-SCALE.md:767-768 and :865 under ruling D5; both resolve. **The two delivery citations in the earlier cell and in docs/UX.md:896 had rotted and are corrected here**: the function is `Model.rowAccessibleName` at Model.js:4570-4587 with the append at :4585, NOT 4213-4215, which is `epgFraction` today; the wiring is Guide.qml:2492-2493, which passes `rowIndex: row.index` and `rowCount: root.rowCount`, NOT :2452, which is `primaryColor` today. Asserted at tests/Model.test.js:1170-1175 and mutation-proven on 2026-09-20: disabling the append turns the node suite red. The one real caveat survives - `rowCount` is what the LIST holds, so under a query it is the capped 200 the header counts against, not the true match total, which stays in the footer. Citing by line is what rotted here twice; cite the symbol. |
| D-A11Y-6 | P2 | M1-09 guide / M2-09 | **The entire empty state was silent to a screen reader** -- no matches, first run, loading, error and no favourites all carried zero accessibility markup, so at the one moment the screen is nothing BUT an explanation, a reader heard nothing. Found by the harness as `L2-Q-05` and measured at the real sink: with a query matching nothing, 0 rows and 0 nodes named the query. Every `grep Accessible.` criterion passed over this for the life of the project, because the markup it searched for was never there to find | **verified fixed at the sink.** `Model.emptyStateAccessibleName` (Model.js:4606) composes the title and prose into one statement and the container announces it at **Guide.qml:2799** - the earlier cell said 2747, which had rotted. 6 node checks at tests/Model.test.js:403-415 call the shipping function, green at 1,427 checks, all 4 mutants caught. Verified by OBSERVATION on the real AT-SPI bus rather than by searching our own source: the harness assertion is `L2-Q-05` at tests/a11y/check_bus.py:347, its RED baseline is recorded at docs/QA-A11Y.md:134, and the same tree went from 4 failures to 3 after the fix, docs/QA-A11Y.md:497-514. Ruling SG1 is what sharpened it from latent to real: whole-word matching creates a dead zone where the empty state is the only thing explaining the screen, and the hint added there for the eye had no equivalent for the ear. Two limits kept deliberately. The sink is the harness's own hidden Qt window, so under D-GS-3 no Quickshell window publishes a tree at all and a real screen reader still hears nothing today - this proves our side would be correct if the host were, which is a different claim. And the gate does NOT protect this wiring: measured 2026-09-20, deleting the binding leaves node and the a11y suite green, because the fidelity guard compares the generated copy against the repo source and a deletion from both is invisible, while the bus half that would catch it is deliberately out of the gate. That gap wants its own row; it is not a reason to hold this one open. |
| D-RUNG-6 | P2 | M1-11 bar widget / 0.7.0 | **A contrast claim shipped in 0.7.0 is false on the running bar, and the tests that assert it are green.** D-RUNG-3 reasoned that no theme overrides `bar.text` or `bar.background`, so both fall back to the theme foreground and background and `tests/fixtures/menu-contrast.json` therefore covers the bar. The tokens do fall back; **the rendered bar does not match them.** Measured on rose-pine off a `grim` capture of the running shell: the bar background is `#faf4ed` exactly as modelled, but the bar's full-strength text renders `#a8a3b3` at **2.25:1** where the model says 6.66:1, because the bar draws its content at roughly half opacity and the token chain does not express that. **The ceiling on the real bar is about 2.25:1, so no alpha on that ink can reach 4.5:1**, and the 0.7.0 note saying the idle glyph is "now clear of it in all 23 installed themes" is wrong | **open, and MY STATED MECHANISM IS WRONG.** I claimed a generated shell.toml overrides the bar with values different from the menu. Three lanes independently checked `/usr/share/omarchy/default/themed/shell.toml.tpl` and I confirmed it: the template writes `[bar] text = {{ foreground }}` and `[menu] text = {{ foreground }}` -- **the same key into both sections**. I read the generated file, saw a colour under `[bar]`, and called it an override; it is the theme foreground, identical to the menu's. The token chain is innocent and my commit message and CONTRAST-RULING.md both say otherwise. The rendered DEFICIT is still unexplained and still real as an observation: it solves to alpha 0.5031 / 0.5000 / 0.5000, one half on all three channels, which points at something drawing bar content at half opacity rather than at a token. But two innocent explanations are now live and neither is excluded: the capture may have caught the 420 ms `barForeground` animation mid-flight, or the peak-pixel method may under-read a thin glyph stroke, which this project's OWN calibration fixture already documents it doing by 7 to 9 per cent on small text. **So the P2 may evaporate entirely.** One capture with the animation settled, against a thick glyph, settles it. Believing the conclusion without settling it is the expensive state, and that is where I left it. The assertions in `tests/Model.test.js` are now confined to the ORDERING of idle against active, which survives a uniform opacity and which the screen confirms (idle measured 2.48:1 against active 2.25:1 before the fix). A threshold claim about the bar needs a BAR fixture measured off the running shell, not the menu fixture. Two further questions this raises and nobody has answered: whether Omarchy's own bar text at 2.25:1 is a host defect worth reporting, and whether our glyph should diverge from every other widget in the bar to be readable |
| D-RUNG-7 | P3 | M2-09 / contrast | The runtime picks a different cursor ink than `Model.cursorInk` computes. On rose-pine the model chooses a 0.69 mix toward the menu text (`#576685`, 4.72:1) and the screen renders `#59537e` at **5.83:1**, which is nearer the menu text itself. The defect and its magnitude modelled almost perfectly beforehand (2.80 predicted, 2.78 measured), so the disagreement is specifically about which ink the mix lands on, not about the arithmetic | open. The direction is SAFE, more contrast than targeted, so nothing is shipped broken. It is filed because D-RUNG-6 was this same class of gap, a model describing a token chain the running shell resolves differently, and that one took a live pass to catch after the claim had already shipped. Candidates: `Color.menu.selectedText` or `Color.menu.text` not resolving to the colors.toml values the fixture assumes, or the runtime fill differing enough to push the mix a step further. One probe printing the three resolved tokens from the running shell settles it |
| D-ID-1 | P2 | M1-06 state / M2-01 sources | **A routine provider password change silently erases every favourite and every recent.** `bin/omarchy-iptv` assigns `t:<tvg-id>` only when that id is unique across the playlist and otherwise falls back to `u:<fnv1a32(stream url)>`, and the stream URL contains the account password. Reproduced through the SHIPPING `assign_ids` by simulating a rotation on the user's real files: **1 id of 3,335 survives** on their main list, 0 of 5, 7 of 48 and 221 of 1,833 on the others. Channel names are byte-identical across the same rotation. Favourites, recents, `lastPlayed` and the player's session record all key on that id, so the loss is total and silent, with no error and nothing for the user to understand. The failed-channel map is NOT in the blast radius, contrary to the brief given to the lane: it is session-only by ruling R11. **The list the plugin is actually configured with is immune**: iptv-org carries a unique `tvg-id` on 1,472 of 1,472 rows, so every id there is `t:` and survives. This bites the moment the user points the plugin at their own subscription | verified fixed at the helper 2026-09-20; the QML wiring's live observation is the residue. Fixed at 482a21e plus the wiring a25382f; two review rounds, six attackers. Terminal re-test: `tests/test_fixture_id_rotate.py` on `tests/fixtures/qa-id-rotate/` - list-v1 (16 rows: six no-tvg-id unique names, one tvg-id shared by an HD/SD pair, four unique tvg-ids, one byte-identical colliding-name pair, one fnv1a32 name collision found by brute force through the shipping `name_id_key`), list-v2 byte-identical except every URL's path segment, and a seeded scheme-1 state in all four slots. Through the real `playlist --state-dir` verb: `moved 8 saved channel reference(s)`, every favourite/recent/lastPlayed/session then resolves by name, the four t: ids and the two refused u: ids untouched, the merged favourite appears once (8 -> 7), the unknown key survives, mode 0600; a second run moves nothing and leaves (size, mtime_ns, sha256) unchanged; on list-v2 every moved and t: reference still resolves to the same name while the scheme-1 ids of all 8 moved rows differ - the harm, reproduced and prevented. The two accepted-limit rows are orphaned by v2 and asserted so: D-ID-2 made executable. Pre-fix (a dead `channel_id_remap`, the refused state) on a scratch copy: 4 of 7 red. The configured list cannot show it (100 per cent tvg-id, remap map empty). Still owed: `--state-dir` observed on the RUNNING helper's argv with `ps` during an active fetch (the only observation of the Service.qml join that would have caught `stateDir: ""`), the guide rendering the merged favourite once, and the migration running on the user's own install (a no-op there by the same arithmetic) |
| D-ID-2 | P3 | M1-06 state | **Accepted, measured limit of the D-ID-1 fix, not a defect in it.** A name key is refused when two channels share it, so a collision costs nothing today. But that is a single-snapshot property and a stable id exists only ACROSS snapshots: when a provider later removes one member of a colliding pair, the survivor becomes the unique holder and INHERITS the key, along with any favourite or recent that meant the row which went away. That is a silent WRONG channel, which is worse than a silent loss because the user cannot see it | open, accepted. Measured on the real lists: 26 colliding name groups on USChannels (55 rows, largest 3) and 39 on SportsPPVAll. Nobody has a second snapshot of these playlists over time, so the frequency is unmeasurable today and designing around it blind would be guessing. What would settle it: keep a dated copy of the user's playlists and re-measure name churn in a month. Recorded rather than designed around, and it is the reason the fix is described as raising survival from 1 of 3,335 to 3,271 rather than as solving identity |
| D-A11Y-1 | P3 | M2-01 Sources | Latent credential exposure on a sink CLAUDE.md rule 5 does not list. `Accessible.description` is correctly masked (`Guide.qml:3172`) but Qt derives an editable field's accessible **Value** from the control's `text`, which rule 5 never considered. `fieldDisplay` (`Guide.qml:1626`) returns the masked string only while the field IS masked, so a revealed playlist or EPG URL publishes its credentials in full, and the Xtream **server** and **username** are not maskable at all so they publish raw always. The comment at `Guide.qml:3171` says a screen reader never gets the query; that is true of the description and false of the value. Unobservable today only because D-GS-3 means nothing is published at all, so this becomes live the moment Quickshell is fixed | **open, half shipped.** Shipped under PO ruling AX1, all pure gain: the false comment corrected, `Accessible.passwordEdit` deleted (inert in both directions on Qt 6.11.2, measured twice by different toolchains, and it left the password with NO label at all, which is also fixed), docs/UX-SOURCES.md 6.7 item 7 and the password row corrected, and CLAUDE.md rule 5 given the correct two-part statement plus the text-event payload as a named sink. **The masking itself was REFUSED under ruling AX2**: the proposed one-line edit was measured to break the Xtream login form outright, a user typing `joe` gets a field holding `e`, because `revealed` is only ever set for URL fields and a masked field is treated as fully selected on every keystroke. Done properly it is five `Model.js` sites plus a UX regression, since a fixed-length mask hides whether the field is even filled while the password below shows true-length bullets. Live risk is zero until D-GS-3 lands upstream, so it is scheduled to that trigger |
| D-CHNO-1 | P2 | M2-03 | Duplicate cycling never advances from the guide, so an HD twin is unreachable by number: three consecutive commits of `301` all report `Channel 301 - Metro Cinema (1 of 2)`. `Model.resolveChno` is correct; the guide hands it the LIVE cursor, which every intermediate digit has already moved (`3` previews 300), so the cycle restarts at ordinal 1. Ruling CN9 inert. Detail QA-RESULTS C7 | fixed at 7a898f1 (merge d55d882, shipped 0.4.0 at 32044ca): the pre-entry snapshot now carries the cursor's channel id and both push and pop resolve against it; 5 node checks plus one QML spec function, 3 red against the parent. NO QA re-test exists: QA-RESULTS has no section written after the 0ef73ed pass that filed it |
| D-CHNO-2 | P2 | M2-03 | A number that does not exist can silently tune elsewhere: typing `20509` auto-commits on the proper prefix `205`, the entry closes and the leftover `09` opens a new entry landing on channel 900 with no error anywhere. Measured 2,690 of 9,484 absent five-digit numbers, 28.4 percent. The IPC `channel` verb is unaffected. Detail QA-RESULTS C7 and ruling CN21 | fixed at de6e57a (merge d55d882, shipped 0.4.0): the unambiguous auto-commit now closes the entry for display only and arms the buffer for the rest of the digit window. Same sample: silent mistunes 2,271 to 0, honest misses 71.6 to 100 percent, instant commits unchanged; 13 node checks, 12 red against the parent. Cost recorded as a Known limitation in CHANGELOG 0.4.0. No QA re-test |
| D-CL-1 | P4 | M1.2 cleanup round | The stand-down journal line `a newer channel change reached the player first (intent %d over %d)` can never print two different numbers: `cmd_play` takes its seq from the lock record, so during a cold start the zap borrows the start's number. All five live stand-downs printed `(intent 662 over 662)` and the like. Diagnostic only; the stand-down itself worked 5 of 5. Detail QA-RESULTS "final live confirmation on d76b649" L6 item 1 | **verified fixed** by the automatic gate. tests/test_player.py `OrderingTest.test_the_stand_down_line_names_the_channel_given_up_and_the_one_left_playing` and `test_the_stand_down_line_says_so_when_the_newer_change_is_the_same_channel` race a real `bin/omarchy-iptv play` into a real `bin/omarchy-iptv player start` held open at its mpv handshake, and assert the helper's own stderr line -- the actual sink; `StandDownNoteTest` (6 cases) calls the shipping `stand_down_note` directly for the shapes a stubbed cold start cannot reach. Re-proven red 2026-09-20: reverting cbfa0d2's helper hunk in a scratch tree brings the live-observed defect back verbatim, `a newer channel change reached the player first (intent 9 over 9)` -- the same number twice -- and both end-to-end cases fail. In the gate via `unittest discover -s tests` (PY_TESTS_MIN 423, zero margin). The sink is a journal line, so no screen is owed, and the row already records the stand-down mechanism working live 5 of 5; only the text was ever in doubt. Citation corrected: `stand_down_note` is bin/omarchy-iptv:3138 (call site 3377), not 2886-2895 -- those numbers had drifted into `settle_socket`'s docstring. |
| D-GS-1 | P2 | M2-09 | `Failed HH:MM - Space to retry` on the CURSOR row measures 3.78:1 glyph-body and 4.86:1 peak against `Color.menu.selectedBackground`, under the 4.5:1 threshold, while the same string off-cursor measures 4.79:1 and 7.13:1. It is the only text on the card under AA and the one that names a key. Detail QA-RESULTS "M2-09 ... on 030ba9b" section 8 | **verified fixed** at ab9c4cd (ruling GS8, merge 05bdf4e, shipped 0.6.0 at f4fd4bc), by the gate rather than by a screen. The rung decision is CALLED, in both engines that ship it: node `tests/Model.test.js:830` and the QML spec `IptvModel::test_guideAtScaleNoticeEmphasis` (`tests/Model.spec.qml:342`). The ratios are computed with the shipping arithmetic (`Model.colorOver` / `relativeLuminance` / `contrastRatio`, aliased at `tests/Model.test.js:1091-1096` so no second implementation exists) over the 23 installed theme token sets in `tests/fixtures/menu-contrast.json`: `:835` pins retropc, the theme this defect was measured on, at 3.54 / 3.52 before and 10.82 / 9.70 after; `:1112` pins 20 of 23 themes under 4.5 before and 0 after, floor 5.94 selected; `:1124` pins the notice as never less legible than the channel name beside it, where it was weaker in 22 of 23. The model is calibrated against real pixels -- `tests/fixtures/contrast-calibration.json`, grim captures of the running guide, six samples, max error 0.15 and optimistic in every one, the shipped full rung itself measured at 10.78 (retropc) and 6.98 (catppuccin-latte). Both slots the notice can land in take the rung from that function (`Guide.qml:2580`, `Guide.qml:2636`), asserted positively and negatively at `tests/Model.test.js:1143`, and no ancestor in the delegate applies an opacity. Green at the check.sh floors: node 1427, python 423, qml spec 68. This retires the WORDS only: the alert glyph beside them still draws at 0.8 (`Guide.qml:2598`), which computes under 4.5:1 on the cursor row in 16 of 23 themes (floor 3.25, above the 3:1 non-text threshold) -- that needs its own id, not this one. |
| D-GS-2 | P3 | M2-09 | With guide data configured but no `tvg-id` matches, every row is 52 px with an EMPTY second line: 1 of 3,335 channels carries a tvg-id, `rowDetail` has nothing to print, visible rows drop 12 to 9 and pageSize 11 to 8. Density spent for white space; it reads as a rendering fault. Detail QA-RESULTS M2-09 sections 5 and 8 | verified fixed at the terminal 2026-09-20; display residue named. Fixed at 9d236d5 with follow-up ef6c307 (ruling GS9, merge 05bdf4e, shipped 0.6.0): `Model.epgCoverage` counts the rows guide data can fill and `rowsHaveDetail` takes `epgCarries` instead of `epgConfigured`. Terminal re-test: `tests/test_fixture_gs2.py` on `tests/fixtures/qa-gs2/` - one group, 60 rows, tvg-id on every row, through the real helper (`playlist` then `epg`) and the shipping Model.js via verify.js: (a) a disjoint guide -> matched 0, carries false, 60 single-line rows; (b) matching -> carries true, 60 filled; (c) exactly one match -> carries true, 59 blank, recorded not judged; (d) the playlist's own guide three days stale -> helper matched 60 at the alias level but nowCount 0 and ZERO entries written to epg-now.json, so Model.js sees an empty map: carries false, 60 single-line. Added by the fixture's verifier; it proves the helper boundary (id matches are not fillable rows) and the stale-guide shape the classification warned about. Measured, not assumed: a Model.js mutant counting title-less entries survives this pipeline because the helper never writes one - the guard is in the helper, and (d) observes that, not epgCoverage's own arithmetic. Pre-fix predicate (`epgConfigured`) reproduced on a scratch copy: 4 of 7 red, every row two-line with nothing to print. The configured list cannot show any of this (28 groups, 100 per cent tvg-id). Still owed to a screen: 38 px vs 52 px rows, 12 vs 9 visible, pageSize 11 vs 8, and a live source switch re-measuring rather than inheriting (ef6c307), today held only by the regex checks at tests/Model.test.js:710-724 |
| D-GS-3 | P3 | M2-09 | Empty accessibility tree: with the guide open on 3,335 rows the shell's AT-SPI root (`quickshell`, role application) reports `ChildCount 0` and `GetChildren` empty, both with toolkit-accessibility enabled at runtime and enabled from startup, while a GTK client on the same bus in the same moment reports a child. No screen reader can announce any row, failure state or `row N of M`. Detail QA-RESULTS M2-09 section 6 | **cause settled 2026-09-15, not ours** (docs/ACCESSIBILITY-INVESTIGATION.md section 6). Measured with the gate open, selecting the connection by `ToolkitName` rather than by name, and confirming window mapping from the compositor: a plain Qt 6.11.2 QML window publishes `ChildCount 1`, a Quickshell `PanelWindow` publishes 0, and a Quickshell `FloatingWindow`, an ordinary xdg-toplevel, also publishes 0. So Qt's bridge, the bus and the gate are all fine, the break is Quickshell, and it is NOT specific to layer-shell surfaces as everyone including this project assumed. The reproduction contains none of our code. **Both upstream reports filed 2026-09-15**: Quickshell https://github.com/quickshell-mirror/quickshell/issues/1144 (the cause) and Omarchy https://github.com/omacom/omarchy/issues/12009 (the host sets no roles or names at all, a separate and larger problem). Stays `open` on our board, tracking the Quickshell issue, because the plugin's accessibility stays unverifiable until that lands. GS5 stays open. The running shell was never restarted and the gate was restored |
| D-GS-4 | P3 | M2-09 | Clearing a source's EPG URL leaves `epg-now.json`, `epg-status.json` and `epg-window.txt` in that source's cache directory; after the next shell restart `epg.configured` is false but `epg.loaded` is true and the guide-data warning is back in the footer for a guide source the user removed. Detail QA-RESULTS M2-09 section 7 | fixed at 381fedc (ruling GS11, merge 05bdf4e, shipped 0.6.0): new helper action `cache epg-clear --key KEY` deletes exactly those three names under the same guards as `remove`, called from `onActiveEpgUrlChanged` and `updateSource` so both clearing paths are covered. Six python tests plus three node checks, red against e4262fa. No QA re-test |
| D-PIP-1 | P2 | M2-05 | `Model.focusPlayerArgv()` emitted the legacy dispatcher `hyprctl dispatch focuswindow class:omarchy-iptv`, which a Lua-provider Hyprland cannot parse (rc 7). Focus-the-player had been a silent no-op at four call sites since v0.3.0 because `execDetached` discards rc, and `tests/Model.test.js:455` transcribed the broken constant, so the suite was green on the bug and red on the fix. Detail QA-RESULTS "M2-05 GATE pass on c4d4075" | verified fixed. Fix 5e0b71c emits `hl.dsp.focus({ window = ... })`. QA re-ran the A/B on a real window and through the product path at QA-RESULTS "M2-05 picture-in-picture LIVE pass on 5e3a374" section 5: focus moved, and Enter on the playing row left `hyprctl -j activewindow` reporting `omarchy-iptv`. The mirrored assertion is gone from tests/Model.test.js |
| D-PIP-2 | P2 | M2-05 | Design defect found before implementation: section 4.10 decided `dispatch_failed` from `Process` exit status, but hyprctl returns rc 0 for every dispatch aimed at an address that does not exist, and reports real refusals as `warning:` text on stdout with rc 0. rc is non-zero only for a Lua parse error in our own string. Detail QA-RESULTS GATE pass, "A new defect the gate was not looking for" | fixed before it could ship: ruling PIP11 at 6f084ca replaced exit-status detection with a compositor readback, and the built code defines success in exactly one place, `Model.pipVerify` (Service.qml:1030 and :1045). QA could not exercise a refused dispatch on the live pass and recorded that arm unsettled (5e3a374 section 4). Section 4.10 of the design still carries the withdrawn wording; corrected in this round |
| D-PIP-3 | P3 | M2-05 | The player window renders at `0.985 / 0.96` because `--wayland-app-id=omarchy-iptv` misses Omarchy's media-opacity exemption, which matches on class; confirmed by reading the window tags and `hyprctl getprop ... opaque` returning false. Detail QA-RESULTS GATE pass G-11 | open. Rulings PIP5 and PIP13 keep it as its own defect to be fixed on its own terms; no commit changes the class or applies the exemption. README 34ebf80 only describes the translucency and points at `contrib/windows.lua`, an optional rule the user must paste by hand. QA's last word, the 5e3a374 live-pass verdict, is that it remains open on its own terms per PIP5 |
| D-PIP-4 | P2 | M2-05 | After a shell restart while in picture in picture the plugin never re-derives state from the compositor: `status.pip.on` read false on all 30 one-second samples while the window was demonstrably floating, pinned and tagged `iptv-pip`; the bar tooltip line never appears and the next accepted request replies `"was":false`. Behaviour is unaffected because a request re-reads before planning. Detail QA-RESULTS "LIVE pass on 5e3a374" section 9 | fixed at 0d8f2ad (merge 3ec2e93, shipped 0.5.0 at db47c37): `Model.pipDeriveState` and `Model.pipDeriveGate` run the design 4.7 read on player attach, and a read that cannot see our window answers `decided:false` rather than announcing off. No QA re-test: no QA-RESULTS section after 4576c0a covers picture in picture |
| D-PIP-5 | P3 | M2-05 | The D-PIP-1 repair fixed the spelling and kept the selector: `focusPlayerArgv` still named the class only, while every other PiP verb refuses anything but a pid-narrowed address. With a foreign `mpv --wayland-app-id=omarchy-iptv` present, the focus command focused the STRANGER's window 3 times out of 3. Detail QA-RESULTS 5e3a374 section 9 | fixed at d3aece2 (merge 3ec2e93, shipped 0.5.0): `focusPlayerArgv` takes the address `pipFindWindow` resolved, and the class branch is removed from `pipSelector` so `pipExpression` refuses `class:` for every verb, which the node tests assert. No QA re-test |
| D-PIP-6 | P3 | M2-05 | Coverage: the live half of `scripts/dev-harness/pip-scenario.sh` gave 63 PASS and 11 FAIL on the reference machine, every failure cascading from "the service learned the player pid from the player itself" because the harness fake player never yields a pid, so the geometry, ordering, address and readback assertions asserted about nothing. `scripts/check.sh` runs only `check-tree`, so M2-05-04's acceptance criterion was unmet and invisible. Detail QA-RESULTS 5e3a374 section 9 | fixed at 043bebd (merge 3ec2e93, shipped 0.5.0): the scenario plays the `--serve` ffmpeg stream instead of the dead 127.0.0.1:9 channel, adds a P11 restart check for D-PIP-4's case, and `requires_live` now skips honestly instead of silently. No QA re-test |
| D-QA-01 | P2 | M0-08 | mpv's own error line carries the stream URL with credentials, and the last stderr line became the critical notification body and `lastError`, reaching the guide status line and IPC `status`. Violates R12 and UX 6.4. Filed against the scaffold 7a26b6d; table docs/QA.md section 6.1 | verified fixed. Fix 3102a38 adds `redactUrls` at every sink, in main before f03fef2. QA re-ran the exact repro in the first harness pass: QA-RESULTS section 6 privacy sweep with a credentialed URL shows the notification body and `lastError` as `Failed to open stream.example.test`, 0 hits in every sink, verdict pass; SEC-07 pass. ARCH independently records it fixed at docs/SECURITY-REVIEW.md:176 |
| D-QA-02 | P2 | M0-08 | An empty `#EXTINF` title made the raw credentialed stream URL the channel name, rendered in the guide row, bar label, tooltip and notifications and persisted into `state.json` recents. Fixture qa-attrs.m3u; table docs/QA.md 6.1 | verified fixed. Fixes 70d24b9 (helper falls back to title, tvg-name, tvg-id, then a channel ordinal) and 3102a38 (`Model.displayName` also rejects URL-shaped names). QA states it by id: QA-RESULTS section 6 records the entry parsing to `Channel 11` with "D-QA-02 fixed: the name is never the URL"; TC-PARSE-05 pass |
| D-QA-03 | P2 | M0-08 | Multi-group `group-title="Animation;Kids;Religious"` was kept whole as the display group instead of the first segment (ruling R5), so gen-10k.m3u produced 1,068 groups instead of about 120 and the US list gained phantom groups. Table docs/QA.md 6.1 | verified fixed. Fix 70d24b9 splits on ";" and lists under the first non-empty segment while the whole string stays in searchKey. QA-RESULTS first pass TC-PARSE-03 pass (Multi Group Channel under Animation, searchKey keeps the whole string, found by kids) and PERF-01 measured gen-10k at 10,000 channels in 120 groups |
| D-QA-04 | P2 | M0-08 | The scaffold's `IpcHandler` exposed `play stop next prev refresh status`; ruling R9 and the README require `toggle` and `previous`. Table docs/QA.md 6.1, check procedure QA.md 5.8 | fixed at 3102a38: `Service.qml:3736 function toggle()` and `Service.qml:3776 function previous()` both exist and the README documents them. NOT verified: no QA record exists of either verb being invoked, in the harness or on the live shell. The live-shell block exercised play, stop and status only. One live invocation of each would settle it |
| D-QA-05 | P3 | M0-08 | `refreshMinutes` defaulted to 60 with a minimum of 5 in Service.qml and the README, against ruling R2 and manifest.json (360 / 15 / 1440), and `barLabelMaxWidth` was not read by the widget at all. Table docs/QA.md 6.1 | verified fixed. `Model.js` SETTING_RANGES is 360/15/1440, `BarWidget.qml:38` clamps `barLabelMaxWidth`, README reads integer 15-1440 default 360. QA-RESULTS first pass TC-CFG-07 pass (clamps automated, 360/15/1440) and TC-CFG-10 pass (label implicitWidth measured at settings 60, 180 and 600) |
| D-QA-06 | P3 | M0-08 | `gzip.decompress` had no inflated-size cap, so a 100 KB gzip bomb inflated past 100 MB in memory; the 64 MB cap applied to the compressed download only, and the EPG path had the same risk. Table docs/QA.md 6.1 | verified fixed. Fix 70d24b9 inflates through the same 64 MB cap; `bin/omarchy-iptv` `decode_text` cites D-QA-06 in its own docstring. QA-RESULTS first pass SEC-11 pass citing `test_sparse_65mb_file_is_too_large_without_reading_it`, `test_gzip_bomb_playlist_is_too_large` and `test_inflated_gzip_bomb_is_too_large` |
| D-QA-07 | P3 | M0-08 | `http://` with no host passed the scheme allow-list (qa-schemes.m3u entry `bad-empty-host`), so mpv failed at play time with a confusing notification instead of the entry being dropped at parse. Table docs/QA.md 6.1 | verified fixed. Fix 70d24b9 requires a non-empty host for http, https, rtsp, rtmp, mms and srt. `tests/test_playlist.py::test_qa_schemes` now pins 13 allowed and 22 entries skipped, where the scaffold gave 14 and 21. QA-RESULTS first pass TC-PARSE-08 and SEC-05 pass on that fixture |
| D-QA-08 | P3 | M0-08 | Decomposed accents were not folded in either normalizer, so a query `cafe` matched an NFD `Cafe` only by substring luck; playlists exported from macOS tools are NFD. Fixture qa-nonascii/qa-unicode.m3u. Table docs/QA.md 6.1 | verified fixed. Fixes 70d24b9 and 3102a38 with shared fold vectors, and 547defa pinned the python and JavaScript sides to the same NFD rule. QA-RESULTS "Regression on 2ce0b52" section R3 BRW records `cafe` on qa-unicode.m3u returning 2 matches, which is the NFC and the NFD row together |
| D-QA-09 | P3 | M0-08 | Option-looking `#EXTVLCOPT` and `#KODIPROP` keys such as `--script` were stored as `options["vlc:--script"]`; stored only in M1 but M2 could apply options. Fixture qa-headers.m3u `optkey.test`. Table docs/QA.md 6.1 | fixed at 70d24b9: keys starting with a dash are refused with a warning. QA coverage is indirect only, first-pass TC-PARSE-04 pass over qa-headers.m3u; no QA text names the id and no assertion is tied to it. ARCH records it fixed by static review at docs/SECURITY-REVIEW.md:97 |
| D-QA-10 | P3 | M0-08 | An empty `#EXTVLCOPT:http-user-agent=` yielded an empty User-Agent header and an empty `--user-agent=` argv item. Fixture qa-headers.m3u `emptyua.test`. Table docs/QA.md 6.1 | verified fixed by the automatic gate. Fix 70d24b9 drops empty header values (`bin/omarchy-iptv` `make_channel`: `cleaned = clean_header_value(value)` then `if cleaned:` before `headers["User-Agent"]`). `tests/test_playlist.py::QaFixtureTest::test_qa_headers` pins it by id at line 730, `assertNotIn("headers", channels["emptyua.test"])`. It calls the shipping parser through `tests/helper_loader.py`, not a reimplementation, and runs on every commit in `scripts/check.sh` step 4 (423 python tests, floor 423). Red against the pre-fix behaviour: removing the `if cleaned:` guard fails that exact assertion with `headers: {'User-Agent': ''}`. The argv half cannot recur - `Model.headerArgs` is fed only from the parsed cache, and the detached player's `header_properties` independently falls back to mpv's default user-agent when the value is empty. The earlier note that no assertion was tied to this id was stale. |
| D-QA-11 | P3 | M0-08 | `Model.splitMpvArgs` used a case-insensitive token regex against a case-sensitive reserved lookup, so `--INPUT-IPC-SERVER=x` was accepted and mpv then exited 1, surfacing as a stream failure instead of the documented console warning. Table docs/QA.md 6.1 | verified fixed. Fix 3102a38 makes the regex lowercase-only and rejects the `--no-` forms of every reserved option. QA-RESULTS first pass TC-MODEL-06 and SEC-13 both pass on the splitMpvArgs cases, and TC-CFG-08 shows the reserved tokens dropped with one console warning. ARCH docs/SECURITY-REVIEW.md:67 |
| D-QA-12 | P3 | M0-08 | Helper error messages embedded the resolved local path, for example a not-found message naming the user's home directory and a refusal naming a /proc path, against UX 6.3 and ARCHITECTURE section 9. Table docs/QA.md 6.1 | verified fixed. Fix 70d24b9 emits the base name only, with the path on stderr. QA-RESULTS first pass SEC-08 pass records the `/proc/self/environ` refusal explicitly as path not echoed, and `tests/test_helper.py:322` asserts the temp path is absent from status and stderr, citing D-QA-12. ARCH docs/SECURITY-REVIEW.md:135 |
| D-QA-13 | P3 | M0-08 | An HTML login page (qa-not-m3u.html) was reported as `empty_playlist` and "no playable channels found", where UX 6.3 distinguishes "Not an M3U file". Table docs/QA.md 6.1 | verified fixed. Fix 70d24b9 emits a distinct `not_a_playlist` code. QA-RESULTS first pass TC-PARSE-09 pass (qa-not-m3u.html gives not_a_playlist exit 1); `tests/test_playlist.py::test_qa_empty_and_not_a_playlist` pins both codes. The guide-side wording half was re-filed as D-LIVE-03 and is verified fixed in the regression on 2ce0b52 |
| D-QA-14 | P3 | M0-08 | BarWidget elided the label at 24 characters instead of `Style.space(barLabelMaxWidth)`, used one glyph for every state where ruling R7 wants three distinct codepoints, and its tooltips differed from UX 6.3. Table docs/QA.md 6.1 | verified fixed in lane B (93eca98, merge 514acef); `BarWidget.qml:38` elides at the clamped `barLabelMaxWidth`. QA-RESULTS first pass TC-BAR-01 to TC-BAR-05 pass with all three glyphs and the UX 6.3 tooltip strings quoted, and TC-CFG-10 pass with the label measured at 60, 180 and 600 |
| D-QA-15 | P3 | M0-08 | IPC `status` returned the full stream URL with provider credentials on the console; ruling R12 says never beyond scheme and host. Flagged as needing a PO ruling because ARCHITECTURE section 5 lists `path`. Table docs/QA.md 6.1 | verified fixed. PO ruled it on 2026-09-12 (STATUS decisions log: IPC status carries no stream URL); fix 3102a38 reports `sourceHost` and an id, name, group and launchedFrom only. QA-RESULTS first pass section 6 shows the status verb with a credentialed channel playing and 0 hits; ARCH docs/SECURITY-REVIEW.md:173 |
| D-QA-16 | P3 | M0-08 | Every notification used a single could-not-play headline, critical urgency and one glyph, and the refresh and EPG notifications did not exist; UX 6.4 wants distinct headlines, normal urgency, a different glyph and a replace-id. Table docs/QA.md 6.1 | verified fixed in lane B (`Model.notifyArgv`, merge 514acef). QA-RESULTS first pass section 5 records the observed argv for all four events with fixed replace-ids (74011 to 74014) and states the texts match UX 6.4 verbatim; the S-04 round later added the leading-dash and quoting guard, itself verified fixed in the regression on 2ce0b52 |
| D-QA-17 | P3 | M0-08 | README carried the ARCHITECTURE draft keyboard map (Ctrl chords, Tab cycles groups), a settings table that disagreed with R2, no note that `omarchy plugin disable` drops inline settings, and no `previous` or `toggle` verbs. Table docs/QA.md 6.1 | verified fixed 2026-09-20, after one of its four halves turned out to be FALSE. The row claimed the README carried `previous` and `toggle` in the command list. `previous` was there; the plugin's own `toggle` verb never was. The README documented the HOST's `omarchy-shell shell toggle <id>` and the `pip toggle` mode, and the claim had been graded by a grep that hits both of those - CLAUDE.md rule 14's failure mode, found by the phase-3 desk pass, not by a test. `Service.qml:3758` ships `function toggle(): string` on the IpcHandler and it is now documented at README.md:128, distinguished from the shell verb above it. The other three halves verify by reading: the two-mode key table, the 15-1440 default 360 settings row, and the disable note (that half re-filed as D-LIVE-13 and verified in QA-RESULTS "Regression on 2ce0b52" R2). Nothing here needs a screen. |
| D-QA-18 | P3 | M0-08 | The `Content-Encoding: gzip` branch in `read_http_source` was dead code, both branches returning `data`; harmless because `decode_text` sniffs the magic bytes. Hardening item with no user-visible effect. Table docs/QA.md 6.1 | fixed at 3c392a2 (lane A, merge f03fef2): the branch is gone and `decode_text` does the sniffing under the 64 MB cap. No QA case covers it and none was written; state established by reading the shipping helper and the removing commit |
| D-SG-1 | P3 | M2-03 | On a single-group list `searchKey` is name plus group, so every key ends with the group name and any query that is a substring of it matches ALL 3,335 channels: `st`, `sta`, `ni`, `es`, `tes`, `ited`, `unit` each report total 3335. Ranking still puts the real answers on top, so it is not a correctness failure; the reporting is. The header said "in All - 3,335 matches" and the footer "First 200 of 3,335 - keep typing" when 19 channels genuinely match `starz`, with 3,316 irrelevant rows one arrow-key below. Detail QA-RESULTS C6, filed in C7 | verified fixed at the terminal 2026-09-20; display residue named. Fixed under PO ruling SG1: `matchRank` tier 3 requires every term as a whole word of the search key (`containsAllWords`). Terminal re-test: `tests/test_fixture_sg1.py` on `tests/fixtures/qa-sg1/` - a committed 50-row list in the one group "United States" with names that carry the polluting fragments, plus the 3,335-row list and a 26-group control generated at test time by `scripts/gen-playlist.py` (sha256 asserted), through the real helper and the guide's own call, `Model.filterChannels(candidates, query, MAX_ROWS_DEFAULT, favorites, chnoIndex)` (Guide.qml:731): pre-fix (substring tier 3, the parent commit's logic, on a scratch copy) reports 50 and 3,335 for every fragment of the group name; shipping reports the real name matches (st 302 of which 200 shown, sta 90, ted/ited/unit 0), the dead zone and its hint, the count growing as a group word completes, and seven fragments returning fewer rows on the control. The fixture's verifier found a rule that whole-word-checks only the LAST token survived every check including the node suite; fragment-first queries (`unit states`, `stat states`, `ted states`) now kill it here and at tests/Model.test.js (M4: 1 of 1428), and a hint that names a group unconditionally is killed by the negative case. The configured list cannot show any of this (28 groups). Still owed to a screen: the header and footer strings, the empty state rendering the hint, per-keystroke responsiveness in the QML engine at 3,335 rows; and the groupNames join in verify.js is a copy of Guide.qml:696-700, filed as D-SG-2 |
| D-REL-1 | P4 | release | `bin/omarchy-iptv:58` still says `VERSION = "0.2.0"`, so `--version` prints 0.2.0 and the outbound User-Agent is `omarchy-iptv/0.2.0` while `manifest.json` says 0.7.1. Found by the artifact audit before the 0.7.1 cut. Not fixed in 0.7.1 on purpose: every code blob on `main` is byte-identical to the SHA the marketplace validated, and a version constant is not worth breaking that during review. Fix: read the version from `manifest.json` beside the helper, or pin the constant to it with a test that goes red on drift | open |
| D-REL-2 | P4 | release | 34 comments in the shipped code cite `CLAUDE.md rule N` by name (Model.js 18, Service.qml 8, Guide.qml 5, BarWidget.qml 2, bin/omarchy-iptv 1) and 36 cite `docs/`, `tests/` or `scripts/` paths that no longer exist in an install. Comments, not files an agent discovers, and not imperative; left untouched in 0.7.1 so the code stays byte-identical to the validated SHA, and disclosed to the reviewer as such. Fix in the next ordinary release: cite `engineering rule N on the dev branch` and drop the dead paths; extend `release.py check` to the four runtime sources so it cannot recur | open |
| D-SG-2 | P4 | phase 3 fixtures | `tests/fixtures/qa-sg1/verify.js` collects the guide's `groupNames` with a COPY of the loop at `Guide.qml:696-700` (the sole group off the axis, then each group entry) rather than a call, and the test asserted the result "as Guide.qml collects it". CLAUDE.md rules 12 and 14: a join by copy is verified by nothing, so the counts the fixture hands `groupWordHint` are the guide's only while the copy stays in step. Found by the fixture's verifier. Fix: lift the collection into `Model.js` (e.g. `groupNamesForHint(surface)`), call it from `Guide.qml:696` and from verify.js, and add the node check; until then the fixture README marks the join UNVERIFIED and the assertion message no longer claims fidelity | open |

Found by QA in the dev-harness pass on f03fef2 (2026-09-12/13); full steps, expected-vs-actual quotes and evidence paths per defect are in `docs/QA-RESULTS.md` section 8, results per test case in sections 3-7. Severity per PLAN.md section 6: D-LIVE-01 and D-LIVE-02 degrade US2 and US6; the rest are cosmetic, wording, docs or hardening.

Regression re-test on 2ce0b52 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "Regression on 2ce0b52"): D-LIVE-01..15 and S-01..S-08 all verified fixed with the original repros (23/23, none reopened); 137-case regression sample with no regression; D-LIVE-16..18 are new P3s found in that pass (0 open P1/P2).

Release regression on 502f4b3 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "Release regression on 502f4b3"): D-LIVE-16, D-LIVE-17 and D-LIVE-18 verified fixed with the original repros (45-group and 50,500-entry generated playlists, SIGSTOPped mpv over the harness); 120-case regression sample with no regression, PERF-01..07 within budget, gates green (299 node, 144 python, 21 qml, validate 0); one new P3, D-LIVE-19, pre-existing on 2ce0b52 (0 open P1/P2).

M2-01 Sources pass on 7e13053 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "M2-01 Sources pass on 7e13053"): 196 `SRC-` cases through the dev harness, 181 pass / 10 fail / 4 blocked (live shell) / 1 not run; D-SRC-01..10 filed (3 P2: D-SRC-02 CLI `epgUrl` lost on the next switch, D-SRC-03 CLI `ftp://` stuck in the loading state - a TC-CFG-04 regression, D-SRC-04 a failed never-fetched switch reports `Switched to ...` and no error; 7 P3). Privacy sweep clean (no credential in any sink but `state.json` 0600 and the probe argv); 50-case M1 regression sample passes apart from D-SRC-03; switch redraw at 10k median 17 ms (first cold switch 352 ms). No-go for v0.2.0 until the three P2 defects are fixed and the live runbook (QA-SOURCES.md section 9) has run.

M2-01 regression on d50e364 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "M2-01 regression on d50e364"): D-SRC-01..10 verified fixed with their original repros (10/10, none reopened, no new defect); 190/196 `SRC-` cases pass (0 fail, 4 blocked: live shell, SRC-UI-16 not run, SRC-MIG-07 not re-run), 55-case M1 sample with no regression (TC-CFG-04 renders the error state again); performance unchanged (10k switch median 19 ms warm, guide open 73 ms, helper 10k 483 ms, `state.json` 50 sources 11,387 B); privacy sweep clean; gates green (653 node, 199 python, 29 qml, validate 0). 0 open P1/P2: go for v0.2.0 from the dev harness; the live-shell runbook (QA-SOURCES.md section 9, plus the D-SRC-04/D-SRC-10 live checks listed in R10) remains before the tag.

Live-shell Sources verification on ec4f702 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "Live-shell Sources verification on ec4f702"): the section 9 runbook on the user's machine with the installed v0.2.0 plugin. 13 cases A-M: 7 pass, 2 fail, 1 partial, 3 blocked. Migration (SRC-MIG-10) is clean - v2 / cacheLayout 2, favorites and 5 recents preserved, 0600/0700 throughout, per-source cache, the migrated row shows real counts - and `omarchy plugin update --yes` reports up to date from GitHub. D-SRC-04 and D-SRC-10 are both **verified fixed on the live shell**. The Sources screen, add, edit, label rename, URL masking with `Ctrl+R` reveal, the theme re-skin and restart persistence all match UX-SOURCES. Privacy sweep clean: a userinfo URL reached no sink but `state.json` and `shell.json` (both 0600) - 0 hits in `status`, `qs log`, the journal, notifications, the helper and every row, footer and tooltip. Two new **P1** defects, both live-only: D-LIVE-20 (switching and add-activation never take effect while the footer reports success) and D-LIVE-21 (the guide never leaves the loading state after the active source is removed). Both were masked by the harness's fake `updateEntryInline`. SRC-KEY-11 (mouse) and SRC-LST-09/SRC-UI-20 (narrow card) are blocked on this machine - no pointer-injection tool, and `hyprctl keyword` is unavailable on the Lua config parser - and SUPER+SHIFT+T cannot be driven by `wtype` (the bind is registered and its command works). **No-go for v0.2.0 until D-LIVE-20 and D-LIVE-21 are fixed**, then re-run live cases D, E, I plus SRC-PERF-02/03, which no completed switch existed to measure.

P1 fix verification on 845d445 (QA, 2026-09-13, `docs/QA-RESULTS.md` section "P1 fix verification on 845d445"): the installed clone was moved to `845d445`, the shell restarted, and **D-LIVE-20 and D-LIVE-21 were both re-tested with their original repros on the live shell and verified fixed** - add makes the new source active in 723 ms, four Enter switches arrive in 145-161 ms with guide content, footer transient and `shell.json` all agreeing, a switch to the 1,474-channel source and back needs no refetch, the second and third Enter on the same row report nothing (the `Could not save settings` path is gone), and removing the active source reaches `configured false` in 821 ms with the first-run surface behind it. The fix's own risk surface was driven too: an external `omarchy bar set` reconciles in 636 ms and still wins when it races a plugin-side switch (0 ms / 50 ms / 300 ms after the keystroke), a plugin-side switch still wins after a CLI write, and the host echoing our own value back produces 0 extra switch events and 0 helper runs. Harness fidelity measured independently by checking the pre-fix `Service.qml` (`d153fe9`) into a scratch copy: the corrected scenario is **61 pass / 0 fail** on `845d445` and **44 pass / 23 fail** on the pre-fix service. Regression: 61/61 scenario checks, 69 re-run SRC and M1 cases and the live blocks above, **0 regressions**; D-LIVE-19 verified fixed as a by-product and O11 closed; one new P3, D-LIVE-22 (footer precedence, from the cosmetics lane). Switch redraw is measurable on a real shell for the first time: median 59 ms on a small source, 116 ms median / 147 ms worst at 1,474 channels, guide open 69-75 ms - every budget met. Privacy sweep with a credentialed source clean (0 hits in status, `qs log`, journal, notifications, helper and every rendered string; the only screenshot hit is the deliberate `Ctrl+R` reveal). Gates green (681 node, 199 python, 33 qml, validate 0 on the repo and on the installed clone). The machine was restored byte for byte and proved. **0 open P1/P2: go for v0.2.1.**

M2-02 player pass on 8f9447e (QA, 2026-09-14, `docs/QA-RESULTS.md` section "M2-02 player pass on 8f9447e"): the `docs/QA-PLAYER.md` runbook executed on the reference machine with the installed clone moved to the RC and back. **96 of 129 `PLY-` cases pass, 5 fail, 6 pass with a corrected expectation, 6 blocked, 16 not run**; seven defects filed, **one P1**. The milestone's whole reason to exist is proven: `omarchy restart shell` with a channel playing keeps the **same mpv pid and the same `/proc` start time**, exactly one window at every 100 ms sample, `time-pos` advancing with no reload, and the full identity including `launchedFrom` recovered from the player's own stash - six clean runs, ten consecutive restarts, and a `pkill -KILL`ed shell that ran no teardown at all. S-03 is closed on the first channel and byte-identically after ten zaps; headers reach mpv only over the socket and are cleared between channels; 241,675 `/proc` samples carry 0 plugin-originated needles; the journal carries 0 mpv lines. Spike caveats C1-C10 are implemented as prescribed and the reattach-after-death trap is unreproducible (750 ms). PO-2's `disable` gate passes at 6676 ms and the README's escape hatch stops an orphan in 220 ms with no shell running. PO-3's full procedure marks silently with **zero** toasts and the guide draws the alert glyph U+F0026 and `Failed HH:MM - Space to retry`, not a red row. The superseded-stop regression is closed and the stop ladder completes with **no shell alive**. Every performance budget is met except the wedged-stop one. Blocking: **D-PLY-1** (P1, the interface goes idle while a respawned player keeps playing, from two independent triggers) and **D-PLY-2** (PO-2's `remove` case, which also deletes the documented remedy). The ARCH-P section 15 race was gated as instructed and **reproduces at 47% (14/30)** - D-PLY-4. Harness rule-11 evidence: 49/49 at `8f9447e`, 33/49 against pre-M2-02 `5e90283`. Gates green (885 node, 247 python, 42 qml, validate 0). The machine was restored byte for byte and proved. **No-go for v0.3.0.**

M2-02 fix verification on b16b479 (QA, 2026-09-14, `docs/QA-RESULTS.md` section "M2-02 fix verification on b16b479"): the seven `D-PLY-*` defects re-verified against their original reproductions, with the installed clone moved to the RC and back. **All seven discharged**: D-PLY-1 (P1) does not reproduce from either trigger (A 3/3, B 2/2) and QA counted the journal's relaunch line directly to confirm **exactly one relaunch, never two**, because the harness check for that clause never runs (D-PLY-9); D-PLY-3 is clean 3 of 3 where it failed 2 of 3; **D-PLY-4 re-measured with QA's own unchanged `race-trial.sh`: 30 trials, 30 won, 0 lost**, against 14 lost in 30 at `8f9447e`; D-PLY-5's warning reaches the guide's existing warning line without refusing the option; D-PLY-6's wedged reap is **4236-4263 ms** against the 4500 ms budget, and a controlled A/B with only the helper swapped gives 6187 ms at `8f9447e` vs 4177 ms here; D-PLY-7's screenshots and resume positions land at the documented paths at **mode 600** with **nothing in `$HOME`**; and D-PLY-2 is closed as accepted under PO-8 with the README's recovery tested verbatim, including proof that the pattern's leading `^mpv ` anchor is load-bearing (without it the pattern matches the shell pasting it). Gates green (954 node, 260 python, 47 qml, validate 0); harness 75/0 (now 83; the repaired check plus positive controls) at `b16b479` and 60/20 at `396a69a`, satisfying rule 11 for the four new scenarios. The six corrected expectations still behave as corrected, and PLY-RST-11's focus consequence is now **run** rather than skipped. Privacy: 20,602 `/proc` sweeps and **2,199,888 process samples with 0 plugin-originated needles**; the three disclosed residuals and nothing else; the new option-warning and directories add no surface. Containment: every artifact `700`/`600`, nothing in `$HOME`; the one file outside the documented list is mpv's content-free shader cache (D-PLY-10). Four new P3s (D-PLY-8 pre-existing socket residue, D-PLY-9 test tooling, D-PLY-10 docs, D-PLY-11 a once-seen divergence under a trigger the guide cannot produce), **none gating**. The machine was restored byte for byte and proved. **Go for v0.3.0.**

## Decisions log

| Date | Decision | By |
|---|---|---|
| 2026-09-12 | Delivery form: native Omarchy shell plugin, id `io.github.rmcdavid.iptv`, name "IPTV", installed via `omarchy plugin add <git-url> --enable` | PO |
| 2026-09-12 | Kinds: `bar-widget` + `overlay`; `service` only if the Architect needs a persistent state owner | PO |
| 2026-09-12 | Playback: exactly one mpv instance driven over JSON IPC; never a second player window | PO |
| 2026-09-12 | Sources: one M3U/M3U8 URL or local path + optional XMLTV URL (plain or gz), via manifest settings schema in shell.json (0600); Xtream URL helper deferred to M2 | PO |
| 2026-09-12 | Data pipeline: stdlib-only Python 3 helper does fetch/parse; QML reads compact JSON caches; 10k channels must not stall the shell | PO |
| 2026-09-12 | Locations: config in shell.json; cache `~/.cache/omarchy-iptv/`; favorites/recents `~/.local/state/omarchy-iptv/`; nothing written inside the plugin dir at runtime | PO |
| 2026-09-12 | Theme: all colors from `Color.*` / `Style.*`; no hardcoded colors without a `CONVENTION-EXCEPTION` comment | PO |
| 2026-09-12 | Security: no sudo; never execute playlist content; never interpolate playlist strings into `bash -c`; URLs/headers reach mpv or curl as argv items only | PO |
| 2026-09-12 | Install flow: `omarchy plugin add`, set `playlistUrl`, press the key; keybinding and menu entry are documented one-liners the user adds | PO |
| 2026-09-12 | Keybinding: `SUPER + SHIFT + T` (verified free); `SUPER + CTRL + T` is taken by Activity | PO |
| 2026-09-12 | Multi-group `group-title` (`A;B;C`): channel is listed under its FIRST group; the full string stays searchable | PO |
| 2026-09-12 | QA sources: iptv-org `us.m3u` for live QA, `index.m3u` (11,041) for the 10k gate, Pluto/Samsung XMLTV gz for EPG; unit tests never touch the network | PO |
| 2026-09-12 | Effort unit: S 0.5-2 h, M 2-5 h, L 5-10 h; one agent-session ~= 3 h. Sequencing is by phase/dependency, not calendar | PM |
| 2026-09-12 | Interim assumptions A1-A9 in PLAN.md section 2 stand until ARCHITECTURE.md / UX.md override them; overrides are logged here | PM |
| 2026-09-12 | Test runners: `python3 -m unittest`, `node Model.test.js` (dev-only node), `qmltestrunner` for `*.spec.qml`; all wired into `scripts/check.sh` | PM |
| 2026-09-12 | Schema freeze gate M0-07: after it, any schema/interface change needs a log entry here before code changes | PM |
| 2026-09-12 | Defect severity: P1 blocks a story, P2 degrades, P3 cosmetic; release needs zero open P1/P2 | PM |
| 2026-09-12 | Reconciliation rulings R1-R13 in ARCHITECTURE.md section 12: UX keyboard model (search mode on open, Tab or / to list mode, no Ctrl chords) supersedes ARCH decision 9; result cap 200; searchKey = fold(name + " " + group) with UX 2.5 ranking; card per UX 5.1; bar glyphs per state, name elided at barLabelMaxWidth | PO |
| 2026-09-12 | Settings frozen: playlistUrl, epgUrl, refreshMinutes (default 360, min 15), mpvArgs, showChannelName, maxRecents, barLabelMaxWidth (default 180); read from the bar-layout entry via shell.barConfig | PO |
| 2026-09-12 | mpv stays an attached Process in M1 (ARCH decision 2); PM risk R3 detached-mpv mitigation deferred to M2 | PO |
| 2026-09-12 | IPC verbs: toggle, play, stop, next, previous, refresh, status. Service actions add removeRecent, focusPlayer, zap over the launch-list ring | PO |
| 2026-09-12 | M1 runs as three parallel worktree lanes: FE lane A (bin/omarchy-iptv + python tests), FE lane B (Model.js, Service.qml, Guide.qml, BarWidget.qml, JS/QML tests), QA (docs/QA.md, qa- fixtures, generator script). PO merges, then QA runs the live-shell pass | PO |
| 2026-09-12 | Privacy rulings from QA defects: a channel name never falls back to a URL (title, tvg-name, tvg-id, then Channel <n>); every notification, status line, tooltip, console line and IPC status output is URL-redacted to the host (Model.redactUrls); IPC status carries no stream URL | PO |
| 2026-09-12 | Helper epg gets a --now <epoch> clock seam for deterministic tests; HTML bodies are not_a_playlist; local-path errors show the basename only | PO |
| 2026-09-12 | Live QA pass on this machine: plugin installed by git clone into ~/.config/omarchy/plugins, keybinding and menu row added with backups and restored afterwards unless the pass succeeds (then left in place as the delivered state), theme check retropc -> tokyo-night -> retropc, EPG-matched checks use the generator pair (file://) plus the Pluto XMLTV for real gzip | PO |
| 2026-09-13 | D-LIVE-01 ruling: the 200-row cap applies to search results only; browsing with an empty query must reach every channel in the scope while keeping the 150 ms open budget (virtualized or windowed list) | PO |
| 2026-09-13 | D-LIVE-02 ruling: any epgUrl change, including from empty, triggers the EPG helper; also at service start and after a playlist load when epg-now.json is missing or stale | PO |
| 2026-09-13 | Wording: timeout reason is `Timed out` (no seconds), README states the 60 s download deadline; `not_a_playlist` renders as `Not an M3U playlist`; `Ungrouped` is always last | PO |
| 2026-09-13 | S-01: raw-title prefix applied to mpv `title` only (force-media-title is not property-expanded by mpv, verified live) | FE, accepted by PO |
| 2026-09-13 | v0.1.0 released and published: github.com/rmcdavid/omarchy-iptv made PUBLIC at the user's request; GitHub release created | PO |
| 2026-09-13 | M2 starts with the Sources lane (user request); Xtream helper and multiple playlists fold into it; detached/idle mpv is lane 2 | PO |
| 2026-09-13 | Sources reconciliation SR1-SR10 (ARCHITECTURE-SOURCES.md): UX names for the view model over state-file names via Model.sourceView; cancelProbe added; mask token **** with type/output visible; architecture caps canonical (label 64, credentials 256); UX 5.4 codes canonical; lane 1 merges first | PO |
| 2026-09-20 | Marketplace capability regression guard: `scripts/check-marketplace-capabilities.py` runs in `check.sh` as step 9, applying the marketplace's transcribed capability patterns to the files its scan actually reads (docs/ and tests/ excluded, root README and its shell fences included). The patterns live in a `.json` file because `.json` is off that scan surface, and the checker reads itself to prove it | PO |
| 2026-09-20 | Branch model: `dev` is the working tree, `main` is the install artifact and nothing else, exported from a clean green `dev` by `scripts/release.py build` from an explicit thirteen-file allowlist, committed onto `main` linearly (omarchy-plugin-update is `git merge --ff-only`) and tagged. Nobody commits to `main` by hand. `release.py check` runs in `check.sh` as step 10 and proves the list is whole: every runtime import and manifest entry point is on it, the README names nothing off it, no agent-instruction filename is on it. Cause: `omarchy plugin add` clones the whole repository, so CLAUDE.md, docs/, tests/ and scripts/ landed in every user's plugin directory; the marketplace reviewer raised CLAUDE.md specifically (issue #7374, 2026-09-20). Version bumped to 0.7.1 because main already carried the D-ID-1 fix under the 0.7.0 label | PO |
| 2026-09-20 | GitHub ruleset `main-is-the-artifact` (#23743896) on `refs/heads/main` only: `non_fast_forward`, `required_linear_history`, `deletion`; enforcement active, no bypass actors, so it binds the owner too. Closes the gap the pre-push audit named: until now the no-hand-commits rule was held by script, gate and tests alone and nothing on GitHub prevented `git push origin X:main`. It does not interfere with `scripts/release.py`, which produces single-parent commits pushed as fast-forwards. `dev` carries no rules. Confirmed read-only via `rules/branches/main` after creation | PO |

## Blockers

- none (live-shell verification completed 2026-09-13 with the user's permission)

| Since | Task | Blocker | Owner | Unblock action |
|---|---|---|---|---|
| - | - | none | - | - |

## Next up

**This section used to carry its own ranked list and it disagreed with two
others.** `docs/PRODUCT.md` had a roadmap, `docs/PLAN-NEXT.md` had a build
order, and this had a third. A reader had no way to tell which was current, and
all three were stale in different places: this one said fourteen rows were
unverified when the count was thirty-two, and called D-SG-1 open when its own
row two hundred lines above says fixed under ruling SG1 with twenty node checks
and all six mutants caught.

**The single current plan is `docs/ROADMAP-PROPOSED.md`**, approved by the
product owner on 2026-09-16 for phases 1 through 3. `docs/PLAN-NEXT.md` is
retired: all four of its items have shipped or been refused with numbers.

Do not add a ranked list here. Add it there, or the next reader inherits the
same problem.

## Handoff notes

Append newest first. Template:

```
### <Task ID> -> <state>  (<role>, <date>)
Files: ...
Commands: <cmd> -> exit <n>, <key output / numbers>
Open issues / deviations / assumptions: ...
```

### M0-03 -> done  (PM, 2026-09-12)
Files: docs/PLAN.md, docs/STATUS.md (docs/QA-ASSETS.md incorporated: A8, M0-08, M0-09, M1-02, M1-04, M1-07, M1.1-03, R14, G3)
Commands: `LC_ALL=C grep -nP '[^\x00-\x7F]' docs/PLAN.md docs/STATUS.md` -> no output (ASCII-only)
Open issues: ARCHITECTURE.md and UX.md absent at write time; PLAN.md carries
`[ARCH-SLOT]` / `[UX-SLOT]` markers and interim assumptions A1-A9 for the
Architect and UX Designer to confirm or override (log overrides above).
